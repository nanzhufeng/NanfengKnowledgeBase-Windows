# 南枫知识库开发长期规则

## 工作边界

- 默认使用简体中文，称呼用户“南烛枫”。先给结论，再给证据、影响范围与验证层级。
- 当前三大用户入口固定称为“主题洞察”“全部笔记”“主题管理”；内部兼容标识和历史页面名不得重新成为用户可见合同。
- 开始改动前依次阅读本文件、`docs/CURRENT_HANDOFF.md`、相关领域文档和当前代码；动态进度、数据统计、验收程序位置只写在交接文档，不写入本文件。
- 代码、可重复执行的测试结果和当前真实环境证据优先于旧文档。发现冲突时记录冲突、影响和依据，不静默改写业务结论。
- 一次只处理一个明确目标；保护未提交成果和真实数据。除非用户明确要求，不修改业务代码、正式数据库、发布资产或远端状态。

## 架构与数据边界

- 保持本地优先：持久化由 Rust + SQLite 负责，界面通过 TypeScript 服务层调用 Tauri 命令；不要让组件直接拼接数据库语义或绕过服务层。
- 修改记录、来源、主题、洞察、导入或附件前，先追踪完整链路：迁移/仓储 → Tauri 命令 → 服务封装与校验 → 调用界面 → 定向测试。
- 数据库变更使用新的、可前向执行的 migration；不得修改已发布 migration。先做迁移预检、备份/恢复与完整性验证，再接触正式库。
- AI 输出是可审阅的派生结果，不得静默覆盖原始来源或用户编辑；保留任务、模型路由、应用、撤销和失败边界。
- API 密钥只经系统凭据库管理；不得写入 SQLite、前端状态、日志、测试快照或文档示例。

## AI 与 Prompt Cache

- 模型和供应商路由在任务创建时冻结；页面切换、目录刷新或设置变化不得改写在途任务。
- 缓存键只使用规范化后的稳定输入、任务阶段、模型/路由与版本化提示词的摘要；不得把原始正文、密钥或可识别敏感内容写入键、日志或指标。
- 缓存命中只能复用同等语义与相同治理边界的结果。模型、提示词、阶段、归一化规则或输出契约变化时必须失效或版本隔离。
- 任何缓存优化都同时报告命中率、节省 token/费用、延迟、回退率与质量抽检范围；小样本命中不能外推为全量质量结论。

## 交付与验证

- 区分“已实现、定向测试通过、构建通过、隔离验收、真实业务验证、正式发布”；不能用前一层替代后一层。
- 测试按最小充分边界执行。浏览器 E2E 前先只读取端口/进程状态；不得为抢占端口而停止用户进程或改写项目配置。
- 构建缓存与验收程序遵循项目既有单一共享缓存规则。清理、覆盖、迁移、打包、签名、上传 GitHub 或操作正式数据前，先说明确切目标、影响与回滚方式，并取得明确授权。
- 发布时以 Git 标签、唯一安装包、SHA-256、远端下载复验和发布说明构成证据链；未签名安装包必须明确标注其边界。

## 文档与审查

- 业务长期规则写入领域文档；运行状态与下一步写入 `docs/CURRENT_HANDOFF.md`；发布事实写入发布文档。避免复制粘贴造成多份“当前结论”。
- 评审改动时覆盖数据安全、迁移兼容、IPC 参数校验、错误可见性、AI 路由/缓存隔离、可撤销性和回归测试。
- UI 改动遵守现行设计基线与皮肤合同；默认最前景内容卡保持实体且不透明。唯一例外是暗色场景玻璃皮肤，其普通最前景业务卡使用 50% 透明度；暗色实体皮肤、全部浅色皮肤、菜单、浮层、弹窗、输入控件与原生文档画布仍保持实体或原始承托。轻量菜单和浮层必须支持点击外部关闭。
- 禁止连续等亮白线、闭环白描边和统一渐变描边；边界层级必须依靠实体卡、间距、阴影和局部状态色表达。
