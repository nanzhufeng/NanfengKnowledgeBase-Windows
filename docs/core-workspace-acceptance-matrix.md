# 南枫知识库核心工作区验收矩阵

更新时间：2026-08-13

状态词只使用：`通过 / 未执行 / 不适用 / 失败`。范围说明不能用来冒充验证结论。

2026-08-10 AI-only 合同替代本表历史行中的`本地分析折叠回退`、`>65本地自动分类`、`正文派生假设/判断/主题整合/决策草案`和`自动摘要首屏`。这些行保留版本证据但不再是现行产品要求；现行规格以`docs/ai-semantic-pipeline.md`和下方“AI 全库分类修订、主题整合与条件洞察”为准。

| 能力/场景 | 设计确认 | 数据/领域实现 | 最终 UI 集成 | 自动契约 | 构建 | 隔离浏览器/集成 | 可见 BAT | 完整用户链路 | 证据与范围 |
|---|---|---|---|---|---|---|---|---|---|
| 三入口产品组织 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 左侧核心入口已统一为`主题洞察 / 全部笔记 / 主题管理`；旧收录箱、整理工作台、手工标签和独立导入导出不再作为并列导航；内部兼容标识保持不变 |
| AI 自动整理最小闭环 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | API Key 进 Windows 凭据库；设置页的大尺寸二级窗口是全软件唯一模型配置层，模型卡在独立可滚动弹窗中选择。主题管理与主题洞察均不显示临时模型入口；全量、增量、主题洞察及重新开始均按设置页已保存任务路由执行，任务创建与断点续跑使用冻结的实际模型。供应商色条、型号与保守能力定位保持不变；正式 WebView2 下的实际保存与生成仍待南烛枫确认。 |
| AI 全库分类修订、主题整合、断点续跑与条件洞察 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | taxonomy AI 在结构和全部归属确定后为每个有归属笔记的主题生成唯一整合；正文由AI生成，来源ID由最终归属确定性绑定。语义档案、归属和主题整合逐批保存检查点、阶段、Token与已知费用；检测到中断会询问继续或放弃，继续只执行剩余批次，材料快照变化则拒绝续跑。`getAppliedAiTaxonomyHierarchy`是主题管理卡二与主题洞察卡二、选中主题、详情请求和AI整理范围的唯一共享投影；没有已应用修订或遇到未映射遗留项时，两个入口都为空，禁止单侧回退原始分类。v112新增按模型保存主题洞察、分类修订和来源档案；切换模型仅切换阅读/草稿轨道，正式分类仍只有用户应用的一条。`AI 补充新增笔记`只处理同模型新增或正文变化笔记，并仅更新受影响主题整合；缺少同模型基线或档案时明确要求全量生成。Rust定向6/6、前端194/194、TypeScript、核心Playwright1/1通过；真实AI、正式库和真实WebView2未执行。 |
| 设置页卡片与 API Key 可用性 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 五类设置主卡片在1702×1066下共用900px列宽和左右起点；`当前程序`固定在可操作设置之后的最底部。AI自动整理入口是整张可点击的原生按钮，图标、标题、说明和右侧区域均打开同一二级设置；右侧仅有方向箭头提示，不保留独立CTA。桌面实算高度88px、与上方皮肤卡18px/下方设置列表16px；场景皮肤继承设置页一级rgba玻璃背景和blur磨砂，不再误用内容前景卡不透明规则。已保存Key持续显示掩码，保存与模型更新失败分开表达。v131 定向浏览器样板通过；真实WebView2材质观感待南烛枫确认。 |
| AI洞察固定分屏、完整弹窗与无本地回退 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | AI未生成时只显示原始笔记、已确认记录和明确等待/失败态；不生成本地语义回退。结果态固定为AI成果上方1/3、四知识Tab、知识正文下方2/3且只有下方纵向滚动；切换四态后三段坐标误差≤1px。查看全部默认约80%应用区域并可调宽高；所有桌面弹窗默认可人工调整大小。竞争假设、来源链接和四知识排版紧凑统一；单主题整理过程/结果持续到用户确认。v108把所有旋转加载环统一为正圆，并与头部语义图标方盒分离；1702×1066核心E2E通过，真实WebView2未执行。 |
| AI主题来源正文上下文 | 通过 | 通过 | 不适用 | 通过 | 通过 | 不适用 | 通过 | 未执行 | AI上下文不再只传来源标题/类型/日期；主题关联来源的可读正文按来源ID进入模型输入，最多20条、单条最多4000字符、正文区最多60000字符、完整上下文目标上限72000字符。旧AI结果不自动覆盖，需点击`AI重新整理`。Rust102/102、前端227/227、TypeScript、Vite及v97隔离`--prepare / --verify`通过，EXE SHA-256=`D22965A1102B9F9D7CC9CCE81E15D02D2AC01EA2C18289140E8085A40786720D`；真实API与正式数据未执行 |
| AI来源范围信息层级 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | `来源边界/证据边界`继续从总览结论中分离并位于完整成果末端；固定上半区不单独折叠任何内容，超出空间的来源边界通过查看全部弹窗读取。新提示仍禁止在`summaryMarkdown`内混入边界清单，结构化来源ID继续负责追溯。v106核心组件E2E覆盖弹窗内完整来源边界；正式WebView2与真实API未执行。 |
| 一键AI整理全部主题 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 点击后立即打开过程弹窗，显示当前主题、完整队列、逐项等待/请求/重试/成功/失败状态与实时统计；不再只改变按钮文字。请求串行且间隔650ms，后端复用HTTP连接，单次时限150秒；超时、连接中断、解析/结构和空总结自动重试一次，401/403不重试。最终结果仍须用户确认，失败保留真实错误并可只重试失败项。前端234/234、Rust103/103、TypeScript、Vite、进度/失败/重试E2E及v101隔离`--prepare / --verify`通过，EXE SHA-256=`8C75CF833E63515D9F1778DA49D909CEBE4486F1C6AE7975CF174921A4F4D054`；真实API长批次与正式WebView2改善幅度待南烛枫确认 |
| 新笔记待AI分类与判断更新入口移除 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 侧栏辅助入口只保留`我的收藏 / 持续跟踪`；导入和来源页不再触发本地自动整理队列或`>65`评分。新笔记保持原文可读并等待下一次AI全库分类修订，只有审核并应用修订后才改变正式主题归属。正式导入与AI分类用户链路待南烛枫双击BAT验证。 |
| 全部笔记最终布局 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 侧栏已经表达入口身份，正文顶部历史重复大标题与单独动作栏均删除，三张主卡从页面顶部直接开始。搜索/筛选第一行，`全部 / 待确认 / 已归类 / 全部加载`第二行，快速定位第三行；列表卡只显示标题、真实主题、来源和右上角日期。全库正文搜索、历史和全部加载能力保留；1702×1066 E2E通过，正式桌面位置待BAT确认 |
| 来源页本地整理动作移除 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 全部笔记/来源详情不再显示或触发`自动整理`、`本地提取`和本地分类动作，只承担原文阅读、搜索、附件、回溯和人工维护。AI分类统一由主题管理拥有，E2E确认旧动作节点为0；正式桌面用户路径未执行。 |
| 来源正文标签唯一性 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 工具区无意义的`正文预览`标签已删除；`来源正文`标题、搜索、查看详情和长正文截断能力保持。静态合同覆盖长正文分支，1702×1066 E2E确认页面不再出现该标签；正式桌面用户路径未执行 |
| 来源软删除与恢复 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 通过 | 未执行 | Repository 通过 legacy record 回收站状态控制来源可见性；Rust 删除→隐藏→恢复契约通过；新 BAT 尚未由南烛枫验证 |
| 三个可见列表统一面板、卡片、筛选与操作 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | `全部笔记 / 我的收藏 / 持续跟踪`共用面板、卡片、筛选、三键及字段工厂；`updated`仅保留为历史状态和筛选兼容值，不再生成侧栏入口。此前四入口共享组件证据继续有效，当前三入口导航待正式桌面复核 |
| 全部笔记定位与正文搜索 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 顶部/滑块/当前只滚动笔记列表且不切换选中项；普通打开、跨页打开和切换笔记都会清空正文查询/高亮并回到正文顶部，旧证据锚点不再自动滚动；只有本次输入后点击`搜索`或按Enter才定位。正文历史使用独立本地偏好键，点历史项只回填不搜索，可清空且不污染卡二历史。前端208/208、TypeScript、Vite、1702×1066 Chrome E2E 1/1与v69隔离应用`--prepare / --verify`通过，真实WebView2待确认 |
| 全软件悬浮滚轮归属与固定控制区 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 全部笔记、两个辅助记录入口、主题洞察和主题管理的并排独立滚动卡统一声明卡片与真实滚动层；全局唯一捕获监听器按鼠标当前位置路由普通纵向滚轮，不要求先点击或改变焦点，且保留内层滚动区、Ctrl/Cmd缩放、横向滚轮与键盘焦点。南烛枫的真实WebView2截图已推翻v68-v70的固定遮蔽方案：52px矩形伪元素直接制造底部直角白块，`margin-right:-12px`又把右上圆角推出裁切边界。v73按`我的收藏`结构把搜索、组合筛选、显示工具栏、状态行和定位条全部移到滚动列表之外，只有下方列表滚动；删除`sticky`、伪元素遮罩、负边距和私有筛选外壳，保留全部笔记自身筛选功能。前端210/210、TypeScript、Vite、1702×1066定向E2E 1/1及v73 Windows隔离应用`--prepare / --verify`通过；真实WebView2仍待当前BAT确认 |
| 全部笔记→主题洞察反向定位 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | `返回上一级`读取App唯一返回上下文，按`topicId + viewMode + sourceItemId`回到竞争假设、判断演变、主题整合或决策版本的原来源；直接进入全部笔记则默认回竞争假设对应来源。201项前端合同、TypeScript、Vite与1702×1066浏览器流程通过；正式WebView2待确认 |
| 主题管理最终布局 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 首屏承担主题边界、自动归类依据、真实父子层级、别名和可处理事项；重复`编辑主题`已删除。零来源主题保留为资料落点，不进入待处理；无父子差异的顶层叶子主题不再显示占位式`层级与关联`，有真实父/子主题时才展示`主题层级`。自动合同、TypeScript、Vite、1702×1066隔离E2E和v28 Windows Tauri结果见当前交接；正式桌面用户路径未执行 |
| 本地排除规则退出生产 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 主题管理不再展示或消费关键词权重、65分门槛、自动排除或本地规则建议；主题边界、归属原因和置信度由AI分类修订提供。历史个人目录仅保留为测试迁移兼容，不进入Release。v103 E2E确认`自动排除`与`.topic-boundary .exclude`均不存在。 |
| 主题结构上下文编辑与统一弹窗 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 顶部只保留`编辑 / 完成编辑`；领域行、主题行和列表末尾分别承担对应新增/编辑入口，四类详情统一由唯一弹窗承载，行内表单已删除。1702×1066应用内浏览器检查默认态、编辑态、领域/主题/子主题入口与弹窗；1280×720对2个领域头和6个主题行测得无重叠、无横向溢出；Esc与遮罩关闭通过。v36 `--prepare / --verify`通过，正式桌面用户路径未执行 |
| 次级维护返回按钮与滚动边界 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 共享次级维护层统一使用固定控制区和独立正文滚动区；主题管理返回按钮右侧至少留38px，正文裁切边界位于按钮下方至少8px。1702×1066 E2E验证长内容滚动前后按钮与裁切边界均不位移；设置/导入返回按钮属于正常文档流，无同类覆盖问题 |
| 主题洞察：竞争假设条件面板 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 只读取已确认记录或同一次AI成果包；材料支持两种以上合理解释时才生成，证据通过`sourceItemIds`回溯。AI未生成且无确认记录时显示空态，不从正文做本地提炼。 |
| 主题洞察：AI主题综述首屏 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 主题综述由AI成果包生成；未生成时显示`等待 AI 生成主题洞察`，并明确只保留原始笔记和已确认记录。旧`.knowledge-overview`与本地五类摘要不再渲染；v103 E2E确认无本地回退与无横向溢出。 |
| 主题洞察：判断演变条件面板 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 只在AI成果包能以来源证明随时间变化时生成，或读取已确认历史记录；材料不足返回空数组，不按来源日期本地拼接阶段判断。 |
| 主题洞察：主题整合态 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 唯一主题整合由主题管理 AI 随 taxonomy revision 生成；主题管理卡三与本面板消费同一当前已应用正文及自动来源，“AI 重新整理”不另造第二份。待审核草稿不能遮蔽正式内容；旧空修订明确提示回主题管理重新生成并应用。右侧只列整合绑定的全部归属来源，卡片主体原位切换正文，外链图标才进入全部笔记，20条来源列独立滚动。v109核心E2E通过；真实AI与WebView2待确认。 |
| 主题洞察：决策与行动条件面板 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 只在来源明确记载决定、行动或结果时由AI成果包生成，或读取已确认决策记录；不再从普通正文生成本地决策草案，不以`尚未执行`等占位链路凑数。真实WebView2内容质量待确认。 |
| 来源标题与正文附件 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 通用标题继续在读取层从正文派生；用户可在详情标题区显式修改正式标题，Repository事务同步 source item 与 legacy Record。来源对象直接查询 legacy/v4 附件；图片、PDF、文本/Markdown/JSON、音频和视频统一进入软件内最高层预览，不支持格式才提供外部打开后备。正文视频保留原位原生控件，下方大预览动作显示`全屏预览`与统一预览图标；PDF和可预览文本的原位阅读区下方使用相同入口文案与图标，均不再把文件名当作按钮语义。文件名仍在正文预览和最高层标题中显示；音频仅保留原位原生播放与通用文件定位，不提供大预览入口。格式判定、Repository、Rust和生产构建通过；真实 WebView2 PDF/媒体解码仍待桌面验收 |
| 历史资料时间线加载、多格式卡片与预览返回 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 未执行 | 未执行 | 附件目录改为一次批量读取受控附件元数据，避免按来源N次查询；时间线首次只绘制120项，按月多列卡片继续加载，已读分类立即从本地缓存显示并后台刷新。图片/视频/音频/文件使用同一网格，音频保留原生播放、进度和音量，文件只按真实格式提供预览入口。点击资料卡仍立即让全部笔记卡三定位所属笔记，但时间线继续作为来源上下文保留；关闭最高层附件预览只返回原分类、查询、展开批次、滚动位置与触发卡焦点，`Escape`不得一次关闭两层。TypeScript、前端定向合同与Vite通过；真实WebView2播放和返回层级仍待桌面确认。 |
| 全部笔记正文与附件回看缓存 | 通过 | 不适用 | 通过 | 通过 | 通过 | 未执行 | 通过 | 未执行 | 正文按32篇/8MB双上限LRU缓存，当前正文完成后才在空闲期预取前后各2篇；已物化附件同样按来源在当前会话保留32篇，回看刚打开笔记不重新显示加载占位。所有普通读取等待态统一显示`正在加载中..`。前端合同、TypeScript、生产构建通过；正式库长正文和真实WebView2体感待验收。 |
| 图片/视频视觉媒体预览、明确复位与输入所有权 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 唯一`AttachmentPreview + mediaPreviewViewport`按当前软件可用视口适配。图片按原始像素且不强制放大小图，视频按真实宽高允许等比放大；中键和标题栏`适合屏幕`均恢复fit、归零、居中。视频原生元素保持播放/控制所有权，缩放后由独立拖动手柄平移；删除底部64px判断。窗口只保留CSS原生resize。隔离E2E覆盖横屏、竖屏、150%/200%DPI、原生控件命中与复位；真实MP4声音、进度、音量、全屏和物理鼠标待BAT确认 |
| 图片预览原始像素保真 | 通过 | 不适用 | 通过 | 通过 | 通过 | 未执行 | 通过 | 未执行 | 受控附件仍为逐字节原件复制，5项Rust附件合同通过；图片元素按`naturalWidth × naturalHeight`建立渲染面，适配比例与实际像素倍率分离，100%对应原图1:1，小图不会为了填满窗口被放大。窗口变化只更新明确处于fit模式的图片，已主动缩放/平移的custom状态保持；中键重新进入fit。前端191/191、TypeScript、Vite及v53 Windows Tauri`--prepare / --verify`通过；未读取正式图片，原文件与WebView2并排观感待南烛枫双击BAT确认 |
| 四状态内容单一职责与切换视口稳定 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 假设、判断、笔记/来源、决策分别只有一个下半区主展示位置。v106以固定三段分屏替代v104粘性Tab方案：AI上半区、四Tab和下半滚动容器均不随模式切换移动，连续切换四态后三段及`window.scrollY`误差≤1px；四态共享下半区滚动锚点，不各自恢复历史位置。1702×1066核心E2E通过；真实WebView2待当前BAT确认。 |
| 知识过期机制 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 通过 | 未执行 | 过期、待复核和判断依赖风险由读取模型派生且不写库；价值不清晰的时间切片控件已从主题洞察首屏移除，底层日期切片能力未删除 |
| 五套皮肤、共享几何与动态可读性 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 目录固定为`实体工作台 · 雾蓝 / 实体工作台 · 鼠尾草 / 实体工作台 · 暖陶 / 场景玻璃 · 花房 / 场景玻璃 · 奔马`。设置页唯一`暗色皮肤`开关持久化独立`light / dark`偏好，五套皮肤共用该模式，默认浅色。v145.1将五套暗色中性面统一为近黑材质总线：`panel=#101415 / raised=#171d1e / input=#0c1011 / border=#050707`与亮字`primary=#f5fbf8 / secondary=#c2d0ca`，只保留各自强调色。搜索输入固定透明，由外层黑色控件承托；按钮、Portal、回收站、附件、Markdown、来源消息、等待态、图标底板、侧栏图标、列表和选中态仅消费这组令牌，不再保留白底、浅灰或场景玻璃白边。三套实体不使用背景图或玻璃；两套场景在暗色下保留背景与外层玻璃、内部中性卡为黑色实体面。静态合同212/212、TypeScript、生产构建、1702×1066 Portal/图标/选区 Chrome Playwright 1/1、五皮肤亮暗几何契约 Chrome Playwright 1/1及固定验收程序`--prepare / --verify`通过；当前EXE SHA-256=`791EB018611C68966C37591ED69B7070D67A7D7FDE3B2701A1D7F93E1AF8AA81`，真实WebView2观感未执行。 |
| 五入口卡二、卡三与搜索几何统一 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | `主题洞察 / 全部笔记 / 主题管理 / 我的收藏 / 持续跟踪`共用`core-workspace-grid / card-two / card-three`语义角色。1702×1066下卡二沿用全部笔记24%宽度、14px卡间距、24/28/34px外边距，卡三消费相同剩余空间；五次切换实测卡二/卡三四边、宽高与搜索框纵向坐标完全一致，搜索框统一45px。主题洞察与主题管理的搜索、工具、AI动作、状态说明和主题层级均进入卡二承托面。前端210/210、TypeScript、Vite、五入口Playwright 1/1及v144固定共享缓存`--prepare / --verify`通过；EXE SHA-256=`DF4791BE6889656FFCB6B25126468EF7C9D05C34FAEC6663C8E0CEE34085AE66`，真实WebView2未执行。 |
| 全软件材质与纯白前景共同标准 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 三套实体工作台使用铺满全应用画布的低饱和实体承托，两套场景玻璃使用固定共享材质Token、外层单次模糊与低对比边缘渐退。五套皮肤的普通最前景内容卡、列表卡、判断卡、详情卡和卡内白卡统一为`#fff`完全不透明实体面。实体设置页`AI 自动整理 / 数据与存储 / 快捷键 / 当前程序`同为纯白实体卡；列表容器透明，路径与`查看全部`文字区无额外灰底。明确语义色卡和选中态保留状态表达。E2E实算实体画布连续、设置卡`rgb(255,255,255)`、文字动作区透明；v142隔离证据见当前交接，真实WebView2未执行。 |
| 全软件搜索框焦点阴影连续性 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 记录入口、全部笔记、主题洞察和主题管理共用`默认投影 + 内高光 + 焦点环`三层状态；默认与编辑态均保留`--search-control-elevation`，聚焦只叠加焦点环，不再由页面私有`:focus-within`覆盖完整阴影。199项前端合同及1702×1066四消费者/四皮肤Chrome实算通过；真实Windows WebView2点击观感待BAT确认 |
| 全软件紧凑小卡逐卡微交互 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | `lift`与`surface-lift`由`NF-MICRO-CARD-LIFT-01`统一持有：操作卡保留按压，纯阅读卡不伪装点击；当前判断、核心解释、提取依据、支持/反对证据、待验证/有效期、来源时间线、决策四阶段、主题内部卡与待处理项均已逐卡补齐，原知识摘要/列表/主题行/关联来源/设置/回收站继续消费。角色只挂最深层独立小卡，无嵌套；竞争假设、决策版本、主题内容区等父容器明确静止。210/210、TypeScript、Vite、1702×1066 Chrome E2E 1/1及v74 Windows隔离应用`--prepare / --verify`通过，实算确认悬浮一张时兄弟卡和父容器不移动；真实WebView2物理悬停待确认 |
| 记录入口来源详情统一 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 全部笔记、我的收藏、持续跟踪共用`DetailPanel`唯一所有者；历史`updated`记录也沿用该详情，但不生成入口。旧`当前判断 / 已确认事实 / 关键证据 / 待验证问题 / 下一步行动 / 历史版本`展示、空卡、弹窗和快捷编辑已删除，详情统一只保留标题/元数据、来源正文、附件、收藏与来源记录编辑。数据库兼容字段、历史数据、导入导出协议及主题洞察正式判断结构不删除。合同和1702×1066真实夹具E2E通过，正式数据观感待BAT确认 |
| 右侧主体阅读字号 | 通过 | 不适用 | 通过 | 通过 | 通过 | 未执行 | 通过 | 未执行 | 六类右侧正文继续显式接入`right-reading-copy-*`唯一语义字号。按南烛枫最新反馈，正文/表格/Markdown标题从旧`1.3×`版本统一缩小到`0.9`，最终为原始字号`1.17×`；链接、按钮、元数据和左侧列表保持原字号。派生值、真实消费者与链接保护合同通过；1702×1066正式桌面待确认 |
| 四知识状态主要内容与小字密度 | 通过 | 不适用 | 通过 | 通过 | 通过 | 未执行 | 通过 | 未执行 | 四个Tab下删除重复眉题、编号步骤卡、阅读方法和职责说明；正文区统一14px起始内边距，分别直接进入主要对象。二次审计删除主题头重复计数、通用介绍、技术空态、笔记活动状态和决策重复详情网格；仅保留日期、来源、置信度、有效期、证伪条件、真实风险等判断/定位信息。168项前端合同、TypeScript、Vite与v41隔离`--prepare / --verify`通过；正式内容观感待固定BAT桌面验收 |
| 两个辅助记录入口双卡尺寸 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | `我的收藏 / 持续跟踪`共用全部笔记的24%主比例、14px间距和等高双主卡；1702×1066隔离浏览器实测全部笔记左右卡335/1047px，辅助入口339/1059px，关联线仍连接真实卡片边缘；`判断更新`不生成入口 |
| 主题层级共享与数量列对齐 | 通过 | 不适用 | 通过 | 通过 | 通过 | 未执行 | 通过 | 未执行 | 主题洞察和主题管理直接消费同一`KnowledgeTopicHierarchy`；折叠/自动展开、领域标题、主题行、空态、`--topic-depth`缩进和固定32px数量列只维护一份。双消费者与共享所有者合同通过，正式桌面仍待确认 |
| 导出结果居中反馈 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 提示框以视口中心定位并置于普通弹窗之上，使用 2px 橙色闭合边框；`打开原路径`为橙色高对比主动作。1280×720 浏览器实测中心坐标为(640,360)，截图见`docs/screenshots/qa/export-toast-centered-v13.png`；浏览器环境不提供原路径动作，按钮可见性仍需桌面确认 |
| 完整笔记导出弹窗比例与底部说明 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 默认尺寸提升为约90vw×90vh、最大1460×980，并切换到独立v3尺寸身份；正文独立滚动，标题、导出按钮和底部格式说明固定可见。1702×1066与1280×720定向E2E均验证弹窗外层无滚动且说明完整露出；正式桌面用户路径未执行 |
| 来源详情标题动作比例、对齐与首帧稳定性 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | `自动整理 / 返回上一级 / 查看详情`由首帧同步列表项与App返回上下文决定，不再等待主题详情或正文后插入；返回优先读取列表项`primaryTopicId`，查看详情固定占位并在正文就绪后打开。三个动作继续使用30px高、12px文字、13px图标和6px间距并与标题行对齐。v71 E2E人为延迟正文与主题详情各1.2秒，确认延迟前后按钮文字、数量、位置、宽高完全一致；前端209/209、TypeScript、Vite、Chrome E2E 1/1及Windows隔离`--prepare / --verify`通过，真实WebView2待当前BAT确认 |
| 细橙关联线、独立滚动与布局稳定 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 普通记录、全部笔记、主题洞察和主题管理统一由`measureCardToCardConnector`按两张卡片真实边缘测量；来源列表滚动会先清除旧线，且选中卡只有完整处于粘性筛选区下方和列表底边上方时才重建关联线。3 条可见边界合同与隔离 E2E 层级断言通过；正式桌面滚动手感仍待确认 |
| 选中项橙色设计边框 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 中栏选中项使用完整橙色渐变边框和锚点设计，不再只有单独一条细线 |
| 自动分类优先、人工仅例外 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 最高候选严格 `>65` 自动归类并可撤销；`45–65` 保留候选，`<45` 人工处理；新导入与历史待整理使用同一裁决 |
| 设置内批量导入导出 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 独立导入导出导航已移除；设置内只保留一个`批量导入与导出`数据交换入口，设置页不再重复提供`导出全部 JSON / 打开导出目录`；场景皮肤下切换条、拖放区与按钮改用同一磨砂橙色语言 |
| 数据交换、完整备份与高级维护分层 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 数据交换页只保留导入与内容导出；`返回上一级`直接重开数据与存储弹窗。该弹窗默认1040×760、使用独立v2尺寸身份、右下角可调节且内容区无横向溢出；数据交换与备份恢复改为等宽等高双卡，每卡只保留标题、一句提示和底部操作，高级维护继续默认折叠。主界面容量与设置概览共用唯一刷新入口，应用重新获得焦点自动扫描。161项前端合同、TypeScript、Vite、1702×1066内置浏览器交互/同屏对比和v39 `--prepare / --verify`通过；未点击备份或恢复，真实外部删除后的Windows数据变化待确认 |
| 快捷键弹窗比例与排版 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 快捷键弹窗使用独立尺寸策略，不读取通用弹窗历史放大值；六项快捷键以两列卡片展示，1280×720与1702×1066均居中且无裁切，小于760px转单列。正式桌面用户路径未执行 |
| 导入日志对齐 | 通过 | 不适用 | 通过 | 未执行 | 通过 | 未执行 | 通过 | 未执行 | 成功/跳过/失败统计使用等宽数字靠右；中文任务状态统一放在每行最右列，列宽固定以保证上下对齐 |
| 存储容量展示 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 左侧只保留`知识库占用 / 磁盘可用`两行，统一按 G 显示并整体上移；设置内仍保留详细存储维护信息；未在后台打开正式库 |
| 回收站批量永久删除 | 通过 | 通过 | 通过 | 未执行 | 通过 | 未执行 | 通过 | 未执行 | 回收站新增`全部删除`，必须经过包含真实条数的二次确认；单条恢复和永久删除继续保留；本轮未执行任何真实删除 |
| 全局信息与入口唯一归属 | 通过 | 不适用 | 通过 | 未执行 | 通过 | 未执行 | 通过 | 未执行 | 页面顶层重复的`已保存 + 三点`和竞争假设重复来源锚点已移除；同一信息、状态、来源链接或动作只保留一个明确展示所有者，该规则已固化到全局 App 开发 skill |
| 交互性能架构 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 99 个 Tauri 命令移出 UI 主线程；重复 IPC 合并、来源正文有界缓存、按入口加载、延后搜索/正文解析、Markdown 记忆、关联线帧调度与离屏延迟绘制已接入；125/125 前端、74/74 Rust、Playwright 14/14、200 篇长正文来源性能合同、Vite、v15 Tauri 与内置浏览器交互核对通过；正式 892 条来源桌面仍待南烛枫确认 |
| 正式 migration v4 | 不适用 | 通过 | 不适用 | 通过 | 通过 | 通过 | 未执行 | 未执行 | 仅临时文件库验证，正式 `D:\南枫知识库` 未执行 |
| 精确去重与统一来源目录 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 通过 | 未执行 | provider稳定ID或标准化可见正文只保留一篇笔记，多次导入追加原件origin；相似标题不自动删除。三个可见列表共同读取可重命名Source Collection，历史`updated`记录保持兼容，零散文件归入`零散文件导入`。前端与Rust合同通过；正式migration v5与真实桌面重命名往返未执行 |
| 单篇文件标题优先级 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 通过 | 未执行 | 显式标题/非日期显著标题/有效文件名优先于正文首行；真实`052…md`结构定向合同确认恢复“052 个人八字丙午年壬辰月”而非“2026年4月5日”。新导入与历史v5回填均覆盖；正式数据未写入 |
| 知识摘要完整内容弹窗 | 通过 | 不适用 | 通过 | 通过 | 通过 | 未执行 | 通过 | 未执行 | 四张摘要卡继续显示两条紧凑预览，底部统一`查看全部`打开同一弹窗并完整渲染该类别全部摘要；支持关闭按钮、Escape、点击遮罩、焦点恢复、弹窗内滚动和进入对应深读页。前端169/169、TypeScript、Vite与v42 `--prepare / --verify`通过；EXE SHA-256=`9872FC09219725AF686DFC1D97594FF60F925A89836EF22074D1FE15A3314136`。未执行Playwright或正式Windows桌面点击链路 |

| 高频列表滚动渲染稳定性 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | v76 以`fixedVirtualList`统一限制全部笔记、普通记录、收藏和跟踪的实际DOM窗口；v85补齐条件挂载生命周期，切回全部笔记时重新测量真实视口，不再停留在零高度产生的7行保底窗口。3,000条范围和首/中/末定位合同继续通过；前端216/216、TypeScript、Vite、1702×1066卡二首屏定向E2E与v85隔离构建/verify通过。完整历史E2E仍有附件分类弹窗关闭流程的既有失败；正式WebView2待南烛枫双击确认 |
| 搜索、定位、Tab与选中态反馈 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 历史项固定8px小圆角矩形；搜索入口、顶部/当前、知识状态Tab统一暖橙悬浮；选中列表标题与卡面提高区分度。布局和大面板不移动 |
| 全部笔记历史搜索与应用右键上下文 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | v81让全部笔记、收藏和持续跟踪共同消费`UnifiedHistoricalSearchScope`，全部笔记并行接入正文与附件索引；浮层改为三列两行范围网格，组合筛选改为两列。右键读取当前`KnowledgeInboxItem`上下文，复制/搜索标题/导出不再因旧`selectedRecord`为空而变灰；右键搜索通过一次性`requestId`真实驱动全部笔记搜索框。前端212/212、TypeScript、Vite、1702×1066 Chrome E2E 1/1及v81隔离应用`--prepare / --verify`通过；正式WebView2附件命中与导出待南烛枫双击BAT确认。 |
| 一键优化数据占用 | 通过 | 通过 | 通过 | 通过 | 通过 | 不适用 | 通过 | 未执行 | 扫描只读；只处理可完整证明无`attachments`记录或导入清单引用的受控附件、完全重复或超时失败备份、受控 WAL checkpoint，以及达到32 MiB阈值的SQLite空闲页。执行前后复核完整性和附件引用，不再额外创建永久“优化前快照”；笔记/知识/版本/导入原件/完整迁移备份与语义相似候选均不自动删除。Rust安全合同通过，正式数据未执行。 |
| 小卡片悬浮落影与裁切 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | v77恢复场景皮肤被选择器覆盖的悬浮底部投影，并为决策链横向滚动容器预留完整投影安全区；主题内容/待处理空态、设置存储路径、7张统计卡、3张功能卡和5张高级维护动作卡逐卡消费角色。大阅读面板、分组容器和决策版本保持静止。前端211/211、Playwright17/17、TypeScript、Vite与v77`--prepare / --verify`通过，EXE SHA-256=`8E65559906F544C5F15DCEFB2E8B5E6E320B6058BE1D7A763D3061144689142B`；真实WebView2待南烛枫双击BAT确认。 |
| ChatGPT附件完整目录、默认加载、月份时间线与正文定位 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | v89替代v84逐项点击恢复：全部/图片/视频/音频/文件筛选读取正文声明与受控实体并集，未物化文件也立即出现；时间线按月分组并可独立滚动。打开当前笔记后按唯一ID顺序自动恢复所有缺失实体，单项失败隔离；图片、音视频、PDF/TXT直接原位加载，不再显示“点击恢复”。前端219/219、Rust两项定向合同、TypeScript和Vite通过；正式WebView2与正式ZIP自动加载待南烛枫用v89 BAT确认。 |
| 图片缩略图库与视频代表帧播放入口 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 图片/视频月份时间线改由`AttachmentTimelineMediaCard`显示真实缩略图/视频代表帧；缺失媒体在分类打开后按来源顺序补齐，点击复用`AttachmentPreview`打开或播放。前端220/220、TypeScript、Vite和v90 Windows Tauri隔离应用`--prepare / --verify`通过，EXE SHA-256=`9CFC6F379FE8DD0304C7821FC1BB8891AB7656DC4673AD3CC305DC816FEF9D36`；真实WebView2代表帧、正式ZIP物化增量与物理点击待南烛枫确认。 |
| Markdown受控解码与语义阅读预览 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | `.md/.markdown`不再进入原始iframe：后端受控读取并按UTF-8/BOM/UTF-16/GBK解码，正文与最高层预览共同使用`AttachmentTextPreview + MarkdownContent`；普通文本自动换行。前端221/221、Rust96/96、TypeScript、Vite和v91 Windows Tauri隔离应用`--prepare / --verify`通过，EXE SHA-256=`61CB55542E7544A6EC40075D83151DBD2E7E8D969767F8D5CC2CAD7974CD86B0`；真实WebView2与截图中的正式Markdown附件待南烛枫确认。 |
| 全格式附件在资源管理器中定位 | 通过 | 通过 | 通过 | 通过 | 通过 | 不适用 | 通过 | 未执行 | 图片、视频、音频、PDF、Markdown/TXT、归档和其他已受控文件在正文原位操作行与最高层预览共同显示`定位文件`；前端仅提交附件ID，Rust从数据库取记录并复用受控目录校验，目录外伪造路径无法打开、删除或定位。Windows使用Explorer直接选中文件，不移动或改写附件。前端222/222、Rust96/96、TypeScript、Vite和v92 Windows Tauri隔离应用`--prepare / --verify`通过，EXE SHA-256=`694284AC80C52CFF72ACF1908A1349B39A164092C7C8D9BE69A087F0EAACB89F`；真实Explorer选中效果待南烛枫双击BAT确认。 |
| 附件目录完整与预览资源有界 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 目录、月份组和全部卡片壳立即完整挂载；实际图片、音视频、PDF和文本预览只在距可视区360px内创建，图片lazy，音视频metadata，PDF lazy。48个PDF隔离E2E确认初始只创建近区iframe，滚动后按需增加且目录始终完整；真实WebView2多视频/多PDF/大图同笔记压力待BAT确认。 |
| 右键菜单作用域 | 通过 | 不适用 | 通过 | 通过 | 通过 | 通过 | 通过 | 未执行 | 自定义菜单只接管笔记内容区；输入、文本域、可编辑区、链接、视频、音频、iframe、设置和导航保留系统菜单。静态合同与默认E2E通过；真实WebView2剪切、粘贴与视频控件菜单待BAT确认。 |
| 当前程序身份与唯一验收入口 | 通过 | 通过 | 通过 | 通过 | 通过 | 不适用 | 通过 | 未执行 | 设置页显示版本、构建标签、运行EXE大小与运行文件自身SHA-256；根目录唯一中文BAT为`启动南枫知识库-当前验收.bat`，PowerShell构建器使用独占锁并只指向v93当前EXE。并发`--prepare`已在编译前以非零状态拒绝。历史BAT已归档为不可执行证据；旧运行包除仍被PID29668占用的v89外已清理。v93为35,704,320 bytes，SHA-256=`C8F865C3BFC93C748D85084575077C303341BD9B074F920C0F08FEBAEA9B7988`。 |

## 当前结论

- 需求已重新核对：产品核心、对象链、三入口职责、四张功能图和独立皮肤合同见`docs/core-workspace-requirements-traceability.md`。
- 旧方向已明确否决：记录中心、人工分类中心、CRUD 首屏，以及只换导航名称继续修补旧页面。
- 当前代码新增真实正文驱动的知识合成读模型、来源归属附件读取、图片直显和通用标题；移除重复来源区块，并修复高度基准分叉、持久旧锚点和祖先链滚动共同造成的卡片跳动/偏移；正式知识对象和 migration v4 语义未改写。
- 本轮合同一致性复验为前端222/222、Rust96/96、TypeScript、Vite、Sites4/4、Playwright默认全量17/17、媒体专项3/3。v93 Windows Tauri隔离应用已通过`--prepare / --verify`，SHA-256=`C8F865C3BFC93C748D85084575077C303341BD9B074F920C0F08FEBAEA9B7988`。自动验证不能替代正式数据桌面可见验收。
- 当前且唯一根目录验收入口为`启动南枫知识库-当前验收.bat`；覆盖当前全部功能与视觉成果。Codex只执行`--prepare / --verify`，不打开正式数据。
- 南烛枫此前真实桌面反馈已固化；正式migration v5仍需获得授权后确认统一来源名称与重命名往返。
- Codex 未在后台打开正式`D:\南枫知识库`；正式 migration v4/v5 和完整用户链路仍未声明完成。

以后任何“完成”结论必须逐行更新本矩阵，不能用单个提交、单次构建或皮肤截图概括。
