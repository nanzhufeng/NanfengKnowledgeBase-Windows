# 南枫知识库验收与回归计划

2026-08-14 v154 AI调用记录：必测：① 设置 → AI 自动整理 → 调用记录可进入、返回设置，空态/读取失败不阻塞关闭弹窗；② 最近50条按时间倒序，逐阶段账本显示模型、阶段、时间、输入/输出/缓存/推理token与成功；无逐阶段账本的历史任务仅显示一条汇总并保留失败/中断摘要；③ 后续阶段失败不得把已成功的前序模型调用误标失败；④ UI、IPC返回值、SQLite账本均不得包含Prompt、正文、API Key、缓存键或稳定前缀原文；⑤ 设置页说明保持简洁，不移除模型路由和成本/缓存判断数据。自动验证：前端220/220、Rust148/148（1项真实Shell按设计忽略）、Vite、Rust格式、`cargo check`、diff检查通过；v154固定验收`--prepare / --verify`与根BAT校验通过，EXE 29,858,304 bytes，SHA-256=`11618D0A319D083C8C5E6BFDF643E739E9B4AE20E07C1C369EF4501DDFE4752D`。正式数据、真实AI、真实WebView2仍未验证。

2026-08-14 v149 AI暂停与单条归属降级：必测：① 40条归属保留唯一合法结果，缩到单条时模型只返回合法主题编号，本地确定性映射真实`sourceItemId/topicKey`，编号越界、置信度越界或空说明拒绝；② 主题管理全库/增量任务点击暂停后不强杀当前请求，当前批次checkpoint保存后停止下一批，弹窗退出且“继续上次生成”可跨重启恢复；③ 主题洞察全主题整理在当前主题完成或重试前暂停，保存剩余ID、累计结果、force和实际模型，继续只请求剩余主题；④ 已应用分类修订变化、剩余主题被删除或设置页改模型，不得把旧队列套到新分类或改写冻结模型；⑤ 单主题一次性请求无伪暂停。自动验证：Rust144通过、1项真实Shell忽略；前端217/217、TypeScript、Vite、Rust格式、diff检查和v149固定验收`--prepare / --verify`通过；EXE 29,777,920 bytes，SHA-256=`3065195EF4E9417EA70322AEB8D5A3737BA21CFB8A367DFFB95F6DA59E37220A`。正式库只读确认任务停在893档案/240归属，未写库、未调用真实AI；来源241的新编号降级仍须南烛枫在v149点击继续后真实验收。

2026-08-14 全库 AI 分类批量恢复与效率合同：必测：① 24 条档案、40 条归属或 8 个主题整合偶发漏项时，逐项解析并保留唯一且合法的已返回项，只补发缺失/重复/坏结构/无效的最小子集；② 整批无有效项、JSON 无法解析或 `finish_reason=length` 时自动二分，单条连续 3 次不完整后停止，不无限请求；③ 恢复后的输出顺序与原输入一致，最终仍须 100% 覆盖，额外 ID、重复 ID、空摘要、无效 topicKey 不得混入；④ 每次获得 usage 的请求都计入任务总量和阶段账本，中断后保存当前检查点；⑤ 千问/DeepSeek 不把动态 ID 和批次数写进 system Schema，动态批次变化时稳定前缀哈希不变；OpenRouter 仍使用动态 enum、`minItems/maxItems` 严格 Schema；⑥ Prompt 合同为 `nfkb-prompt-cache-v3`，持久化结果字段与语义未变，`nfkb-ai-execution-v1` 旧断点仍可续跑。结果：Rust 142/142、1 项真实 Shell 测试按设计忽略；新增恢复、逐项解析与缓存前缀定向 7/7。TypeScript、Vite/Tauri release构建、v148 `--prepare / --verify`与BAT路径校验通过；EXE 29,720,064 bytes，SHA-256=`B651822FC4F32C6EE16E5117B47467243B76574001397F4E8E4F3BA8C3977906`。前端现有 211/213，2 项失败由此前未提交的根 `AGENTS.md` 精简与当前合同测试不一致引起，与本轮 Rust AI 链路无关。未读取正式 SQLite、未调用真实 AI，893/901 来源规模的真实继续生成仍待授权验收。

2026-08-13 Prompt Cache v15风险收口：必测：① 断点、主题洞察、来源档案和增量基线必须与`nfkb-ai-execution-v1`完全一致，`legacy`结果只展示不复用；② 启动恢复把旧`running`任务转为`interrupted`，分类断点保留，后台线程join失败可靠结束任务；③ 多阶段成本只在所有阶段价格已知时聚合，价格未知与缓存节省未知必须显式显示，不得以`$0`代替；④ 千问1024附近估算保持隐式缓存，显式模式采用1536安全阈值，命中只认供应商usage；⑤ migration v15保留行数、外键、完整性和幂等性，来源档案主键包含执行契约；⑥ 2来源真实链路依次覆盖Flash档案、Plus结构、Flash归属、Plus整合，只写验收任务/账本，不写业务结果；⑦ BAT启动前校验EXE、源码指纹与元数据，并清空新旧数据目录覆盖变量。结果：正式schema 1–15、0 running、895记录/901来源/68主题/0分类修订；Rust135/135、前端213/213、TypeScript、examples、格式/diff通过；v147 EXE 29,633,024 bytes，SHA-256=`E5C96CEA77789609139B8BE31BD0E7DBCECB5B10224C248980C6BF935D33D469`，源码指纹`FA6771CA625940ACE64002186F59796E6554B5E247DE72A276C43628BBA37FBC`。未执行901来源全库生成或应用修订。

2026-08-13 Prompt Cache v14阶段历史证据：相同模型、阶段、Schema、推理模式和稳定taxonomy在动态批次变化时保持同一缓存键；模型、Schema、推理模式或稳定上下文改变时换键，键中不含原始材料。千问带标记稳定块必须是独立消息，动态材料位于下一条消息；现行本地显式阈值已由v15提高为1536，1024只是供应商最低值。OpenRouter发送稳定`session_id / prompt_cache_key`并解析实际成本，DeepSeek解析hit/miss并冻结价格。隔离真实API证据：DeepSeek命中`0/7040/7040`；千问首写`7107`、后续命中`7107/7107`；OpenRouter Haiku 4.5首写`12532`、后续命中`12532/12532`。正式v14和2来源缓存/思考A/B通过，v146现已由顶部v147替代，不再是当前验收程序。

2026-08-13 v144 五入口双卡工作区几何统一：必测：① 主题洞察、全部笔记、主题管理、我的收藏、持续跟踪共用同一卡二左/上/右/下边界和全部笔记24%宽度，卡间距14px，卡三消费统一剩余空间并与卡二等高；② 五入口卡二搜索框纵向起点相同，自身高度统一45px；③ 主题洞察和主题管理的搜索、筛选、AI动作、状态说明、主题层级全部位于卡二统一承托面内；④ 切换入口不改变功能结构、卡三内容职责、列表/树滚动、五套皮肤材质与纯白前景标准；⑤ 卡三加宽后四知识状态、决策版本和长内容无横向溢出。自动验证：前端210/210、TypeScript、Vite、1702×1066五入口/五皮肤Playwright 1/1、`git diff --check`和固定共享缓存v144 `--prepare / --verify`通过。唯一当前EXE为29,605,376 bytes，SHA-256=`DF4791BE6889656FFCB6B25126468EF7C9D05C34FAEC6663C8E0CEE34085AE66`；BAT `--path`与元数据一致，未新增版本化缓存、未启动应用，正式数据与真实WebView2未执行。

2026-08-13 v143 文档全屏预览与历史资料返回层级：必测：① PDF、Markdown、TXT等可预览文档的正文按钮统一显示预览图标与`全屏预览`，不再显示文件名；② 点击仍打开既有最高层`AttachmentPreview`，文件名在原位预览与最高层标题保留；③ 视频入口文案/播放语义不变，音频不新增大预览，不支持格式的文件名入口、定位文件、受控存储和附件解码不变；④ 全部附件、图片、视频、音频、文件时间线点击资料后保持挂载，分类、查询、已展开批次和滚动位置不能丢；⑤ 点击资料仍立即让卡三定位所属笔记；⑥ 关闭按钮、预览遮罩或一次`Escape`只关闭最高层预览，返回原时间线及触发卡焦点，不得连关两层；再次关闭时间线才展示已定位笔记。前端全量210/210、定向19/19、TypeScript、Vite和固定共享Cargo缓存中的v143 Tauri编译通过；固定缓存原位`--prepare / --verify`、根目录BAT `--path`与元数据校验通过，当前EXE为29,603,328 bytes，SHA-256=`3C8166F362A3BB042DB77FA748482E8B6353A57F14ECBAFB0B6FCE4096F328F4`。未新增版本化构建缓存；应用未启动，正式数据、真实附件和真实WebView2未执行。

2026-08-13 v142 实体画布与设置卡底层统一：必测：① 三套实体皮肤在主题洞察、全部笔记、主题管理、收藏、跟踪、回收站与设置中，应用画布和主区域消费同一不透明承托底，四周不再露底形成大圆角背景卡；② 两套场景玻璃背景与玻璃职责不变；③ 设置页AI自动整理、数据与存储、快捷键、当前程序为同级纯白实体卡；④ `.settings-list`透明，不形成包裹两个设置项的额外白卡；⑤ 路径与`查看全部`文字动作区透明，无灰色小底；⑥ 实体侧栏容量说明为低一级中性白、数值和刷新图标为纯白，不继承固定蓝灰；⑦ 五入口布局、900px设置列、卡片几何、交互和正式数据链路不变。自动验证：前端209/209、定向63/63、TypeScript、Vite、1702×1066五皮肤核心Playwright 1/1、唯一当前v142隔离`--prepare / --verify`与BAT `--path`通过。EXE 29,604,864 bytes，SHA-256=`935895F516509FD03EA639A827837A7D6F916DD8FE84CD4E6919E02813723134`。应用未启动，正式SQLite、真实AI与真实WebView2未执行。

2026-08-13 v141 五套皮肤与纯白前景：必测：① 皮肤目录只显示三套实体工作台与花房/奔马两套场景玻璃，旧`classic / desert-lantern`偏好迁移到雾蓝；② 三套实体无背景图和`backdrop-filter`，两套场景才使用背景与外层玻璃；③ 三入口保持当前真实结构，全部笔记单一大内容卡、主题管理当前卡二/卡三和侧栏激活项不因换肤改变；④ 五套皮肤的普通最前景卡统一`#fff`不透明，全部笔记卡二普通列表与卡三正文实算为`rgb(255,255,255)`，语义/选中态不漂白；⑤ 主题洞察无会话模型入口，设置页仍是唯一模型配置层。自动验证：前端208/208、TypeScript、Vite、1702×1066三入口/五皮肤Playwright 1/1、v141隔离`--prepare / --verify`与BAT `--path`通过。EXE 29,604,864 bytes，SHA-256=`F4841A7C06AF0EB10746E121F0FBCD454C290F5CE08DEA953401C4183BDBA94B`。应用未启动，正式SQLite、真实AI与真实WebView2未执行。

现行用户可见名称只使用`主题洞察 / 全部笔记 / 主题管理`。测试中的`knowledge / sources / Record / listSourceArchive`是内部兼容标识；历史计数和旧审计中的`收录箱 / 来源档案 / 知识视图`只保留证据语义，不得成为新断言。

2026-08-10 起，AI-only 语义合同替代本文件后文历史段落中的本地自动分类、按算法版本续接、本地摘要/假设/判断/主题整合/决策草案测试。现行必测路径是：新笔记保持待 AI 分类 → 主题管理生成全库修订 → 全部归属确定后生成每主题整合 → 覆盖数、整合和低置信项可审核 → 一次应用并可撤销 → 主题管理卡三与主题洞察共享同一已应用整合 → 主题洞察 AI 生成综述及三个条件面板；AI不可用时原文可读且没有本地生成回退。详细字段与边界见`docs/ai-semantic-pipeline.md`。

2026-08-11 v110新增长任务止损合同：每个成功批次必须在下一次模型请求前持久化结果、offset、阶段和累计用量；任一后续请求失败不得删除先前批次。重启或重新进入主题管理时必须提示继续/放弃/稍后处理，继续请求必须携带原任务ID且只处理剩余批次。主题整合来源必须从最终归属确定性生成，模型返回错误来源ID不得使长任务失败。来源ID或正文哈希变化时拒绝续跑。自动验证：前端192/192、Rust106/106、TypeScript、Vite、1702×1066核心E2E1/1、v110隔离构建与哈希校验通过；正式数据库、真实API和真实WebView2未执行。

2026-08-11 v111新增共享分类投影合同：主题洞察卡二、主题管理卡二、选中主题、详情读取、单主题整理与批量整理必须共同消费`AppliedAiTaxonomyHierarchy`；这个模型仅由已应用AI分类修订投影得到，任何未应用、撤销或无法映射到该修订的遗留领域/主题都不得在任一入口显示。首次使用时两个入口同时显示`待 AI 生成全库分类`；应用修订后两边显示相同领域、主题、顺序、折叠树和可选主题范围。自动验证：前端193/193、TypeScript、Vite、1702×1066核心E2E1/1及v111隔离`--prepare / --verify`通过，EXE 38,598,656 bytes，SHA-256=`C6B9C62C58C163D83512D9F3E12769FF7D82519602B63A0E7D5C4472C290A205`；本次不涉及Rust/SQLite写模型，未重跑Rust测试、真实数据库、真实API或真实WebView2。

2026-08-11 v112新增模型轨道与增量分类合同：主题洞察按模型和输入指纹保存，切换模型只能切换阅读轨道，不能覆盖任一历史结果。主题管理增量路径必须以同模型最新完整分类为基线，只将新增/正文哈希变化笔记送入档案与归属步骤，只重整受影响主题的整合与来源绑定；未变笔记只可复用同模型同哈希档案，禁止跨模型复用。无本模型基线、基线撤销、材料变化后的断点续跑或缺少同模型档案均必须明确停止并要求全量生成，不能暗中回退全量或本地分类。附件定位必须向 Explorer 传递带引号的精确受控媒体路径，含中文/空格时不得回退到默认“文档”目录。自动验证：Rust定向6/6、外部定位参数2/2、前端194/194、TypeScript、核心Playwright1/1及v112隔离`--prepare / --verify`通过，EXE SHA-256=`1B8D216BA71C0680DF79E71CB6E8CFABEC01D0107BC76AE6722D5BB34127A80D`；真实API、正式数据库和真实WebView2未执行。

2026-08-11 v117以原生 Shell 合同替代 v92/v112/v116 的 Explorer 命令行定位：唯一`external_open::reveal_path`必须调用`CoInitializeEx → ILCreateFromPathW → SHOpenFolderAndSelectItems`，源码不得包含`explorer.exe`或`/select,`；附件只提交ID，导出只提交受控`exports`内的实际文件。正文、最高层预览、五类时间线和两种单篇导出都必须命中同一后端服务。自动验证：Unicode/空格/井号宽字符合同、附件/导出越界拒绝、跨入口静态合同、真实MP4原生Shell调用、Rust109/109、前端198/198、TypeScript及v117隔离`--prepare / --verify`通过，EXE SHA-256=`8B618359DF6A446FACA304C86F9548EA13BD1DC9F7DB1A13C36A45B21A2EC1FC`；软件内逐入口高亮仍待南烛枫确认。

2026-08-11 v119修复 v117 的真实桌面定位失败：受控附件路径经 Rust `canonicalize` 会使用`\\?\`长路径，`ILCreateFromPathW`会拒绝该 Shell 不可识别形式；同时`SHOpenFolderAndSelectItems`必须接收父目录 PIDL 与目标文件 PIDL 数组，不能把文件 PIDL 当作文件夹并传`cidl=0`。现仅在已完成受控目录校验之后，为 Shell 将`\\?\D:\...`还原为`D:\...`、将`\\?\UNC\server\share\...`还原为`\\server\share\...`，随后传入父目录和一项选择。自动验证：普通/Unicode/长路径转换 Rust 合同2/2、Rust110/110（另1项真实Shell测试默认忽略）、前端198/198、TypeScript、Tauri v119隔离`--prepare / --verify`通过，EXE SHA-256=`FD5398E24F125DA566FCE8AE5CC5B3BC464C517C83A635CB4B8E61943B91D24B`；正式库和真实应用未由 Codex 打开，仍需当前 BAT 逐类型确认实际高亮。

2026-08-12 v122 回收站反向审计：删除状态的唯一用户读取模型为`visible_source_items`。必测：① Source 关联 Record 移入回收站后，来源列表、主题来源计数、详情、附件时间线和 AI 取材均不出现；只有显式恢复后才重新出现。② 已删除来源再次触发收藏/导出等 Record 侧车时必须拒绝，绝不创建或返回活动 Record；损坏的非空`legacy_record_id`也必须保守隐藏并拒绝替代创建。③ AI 草稿完成/应用/撤销期间若来源被删除或归档，任务必须中断或跳过该来源，且不写入主题归属。④ 前端删除后，来源搜索、附件搜索、附件时间线和详情缓存立即清空；恢复/永久删除后，三个知识入口刷新同一可见性修订。自动验证：Rust111/111（另1项真实 Shell 测试默认忽略）、前端199/199、TypeScript、Vite、release检查与v122隔离`--prepare / --verify`通过，EXE SHA-256=`A034938513005763A9E14958913DA36AE6DBFBF83BF2643F9BEA2B795D979FB1`；待由南烛枫在正式桌面验证“删除→切换三入口→重启→恢复”路径。

2026-08-12 主题管理模型配置收口：必测：① 主题管理筛选行只有`全部 / 待处理`两个按钮，不存在模型标签、模型触发器或`选择 AI 模型`弹窗入口；② 主题洞察与设置页的模型选择保留；③ 主题管理全量、增量、应用/撤销后的草稿刷新均不传递主题洞察的临时模型选择，统一交由后端读取设置页已保存模型；④ 已中断任务仍可继续，且续跑以任务断点保存的模型为准。静态合同、1702×1066浏览器样板、TypeScript、Vite和当前隔离验收包待本轮执行；不打开正式数据库、不调用真实AI。

2026-08-12 v123 数据空间引用对账：必测：① 预览逐项显示未引用受控附件、WAL、可回收数据库空页、重复/失败备份以及受保护附件、完整备份、导入原件的空间；扫描本身不得写文件。② 活动附件和导入清单中的附件绝不成为候选；孤儿文件仅在执行前再次未被引用时才删除，并清理空目录。③ checkpoint 繁忙或任一完整性检查失败必须在删除前停止；WAL 只通过 SQLite checkpoint 收缩，绝不直接删除。④ 仅达到32 MiB阈值时才VACUUM，优化器不得新增永久安全快照。⑤ ChatGPT恢复的临时物化目录必须在成功或失败后清除，已提交附件仍保留在受控存储。自动验证：前端199/199、数据优化Rust4/4、附件Rust10/10、设置页Playwright1/1、TypeScript、Vite和v123隔离`--prepare / --verify`通过，EXE SHA-256=`853C7BDD57501728340F7B2E5A477113A943B41FCCCBB72D25178BA93D7EF76B`；正式数据不得由Codex执行清理，待南烛枫在正式桌面先扫描、核对保留区，再在界面二次确认执行。

2026-08-12 v124 历史附件恢复卡实体白底：必测：场景皮肤下`恢复历史导入附件`卡必须使用固定`#f8fafd`实体浅白底和`#dce3ec`边界，不得引用`--glass-card`；同级操作卡的两列/窄屏单列布局、按钮、文案和悬浮角色不变。自动验证：定向前端20/20、TypeScript、生产构建和v124隔离`--prepare / --verify`通过，EXE SHA-256=`2BA9DDB61C13DBE19FA0A24CEE11BB4F5F5F1E135515B1AD0CC4F9772184D239`；真实WebView2待南烛枫关闭旧验收程序后启动当前BAT确认，Codex不启动软件或打开正式数据。

2026-08-12 v125 数据优化确认与路径防护：必测：① 扫描结果必须带后端签发的单次确认令牌；令牌空、重复使用或超过5分钟均不得执行。② 执行前重扫候选，任何新增、消失或替换候选均须停止并要求重新扫描，原文件保持不变。③ 每次删除前必须重验路径仍在`attachments`或`backups`受控根内，拒绝符号链接与Windows重解析点；目录删除逐层检查，不直接对候选目录使用不受控递归删除。④ 原有WAL checkpoint、数据库完整性、引用图和保留区合同不变。自动验证：Rust 116通过、1项显式忽略（Shell reveal真实系统动作）；前端200/200、TypeScript与生产构建通过；v125隔离`--prepare / --verify`通过，EXE 38,818,816 bytes，SHA-256=`39D4CB2E80CC1604D319B10676DA66604A5E22D5B328FDF722459D40CF4F8246`。Codex不执行正式数据清理；真实WebView2与正式库清理待人工确认。

2026-08-12 v126 存储安全复盘全项闭环：必测：① 令牌为空、重复或超过5分钟，优化一律拒绝。② 同一候选即使保持相同路径、文件大小和修改时间，只要实际字节被替换，内容 SHA-256/目录指纹变化也必须阻断，原文件保留。③ 备份目录在删除前递归预检：任意符号链接、Windows重解析点、越界或非常规节点均阻止删除开始，不产生“先删一部分再发现异常”。④ Asset 协议不得递归授权`attachments`；仅数据库登记且`controlled_attachment_file`重新验证通过的具体文件能被`convertFileSrc`预览，查询、恢复和新增均补单文件授权。⑤ WAL checkpoint、完整性检查、附件引用图、完整备份和原始导入保留合同不变。自动验证：Rust 116通过、1项显式忽略；前端201/201、TypeScript、生产构建、定向安全合同和`git diff --check`通过；v126隔离`--prepare / --verify`通过，EXE 38,836,736 bytes，SHA-256=`5187A11814E678A8FC781AE178438F04B9B8AAEEA581C7B310CCF7EC4E2BEEA3`。Codex不执行正式数据清理；真实WebView2与正式库清理仍待人工确认。

2026-08-12 v127 AI 自动整理二级设置：必测：① 设置首页仅显示一张最大宽度900px的实体浅白入口卡与`进入设置`按钮，不提前显示接入通道、API Key、模型、用量或保存按钮。② 点击入口后原有通道、密钥显示/隐藏、模型选择、保存选择、保存并更新模型和用量统计均在统一二级弹窗内可用。③ 弹窗必须支持遮罩点击和 Escape 关闭，且关闭后默认设置页仍只显示入口卡。④ 场景皮肤下入口卡保持`#f8fafd`实体浅白底与`#dce3ec`边界，不取`--glass-card`透明材质。自动验证：当前合同静态测试38/38、前端全量202/202、TypeScript、生产构建、`git diff --check`与v127隔离`--prepare / --verify`通过；EXE 38,838,784 bytes，SHA-256=`09258D05082038CD5B2F530139B628F5B44F2BCDA0ABAD158B77F44B7D887A28`。真实WebView2路径仍由南烛枫从当前 BAT 验收，Codex未启动新应用、不开正式数据库、不调用真实 AI。

2026-08-12 v128 原始导入归档日常读取边界：必测：① 日常启动、窗口`focus`、页面可见性恢复、进入设置和普通笔记列表刷新不得调用`getStorageStats`或递归枚举`imports/raw`。② 存储摘要只能从经过 schema 校验的本地缓存初始化；缓存缺失或损坏时保持空态，不得为补足展示隐式扫描目录。③ 导入完成、附件新增/移除、数据库或完整备份恢复、数据优化和用户明确点击刷新后，必须重新读取统计并刷新缓存。④ 来源正文、搜索与 AI 继续读取 SQLite 来源表；原始归档仅服务导入保真、备份恢复和明确维护。自动验证：定向合同26/26、前端全量203/203、TypeScript、Vite生产构建、`git diff --check`与v128隔离`--prepare / --verify`通过；EXE 38,838,784 bytes，SHA-256=`1B8428B92C357563ADC3075331E848DD444CE40B5444DC1EA8DC377BB769D81F`。未启动软件、不打开或写入正式数据库；真实 WebView2 首次无缓存空态、显式刷新和导入后统计更新待南烛枫按当前 BAT 确认。

2026-08-12 v129 设置页 AI 自动整理入口：必测：① 入口不得继承通用操作卡的`height:100%`，1702×1066下高度必须为88–92px，图标、标题/说明与`进入设置`保持横排。② 与皮肤卡间距18px、与设置列表间距16px，列宽和左右起点均为900px。③ 场景皮肤入口必须与同页一级设置卡同为`rgba`玻璃背景和`blur`，原版浅色保持实体浅表面。④ 二级弹窗的遮罩/Escape关闭、模型通道、密钥显示/隐藏、保存和用量逻辑保持可用。自动验证：定向合同27/27、1702×1066浏览器几何/材质样板1/1、前端全量204/204、TypeScript、Vite生产构建、`git diff --check`与v129隔离`--prepare / --verify`通过；EXE 38,838,784 bytes，SHA-256=`99382763314B18C369199346FDA27DDEBB1814A24FE88D6504CCEF478671FBB3`。未启动软件、不打开或写入正式数据库；真实 WebView2 由南烛枫按当前 BAT 确认。

2026-08-12 v130 正文视频全屏预览动作：必测：① 视频原位播放器继续保留原生播放、进度、音量和自身全屏控制。② 下方左侧动作不得显示视频文件名，固定显示视频预览图标和`全屏预览`；悬停提示与无障碍名称均应明确“全屏预览并播放视频”。③ 点击必须复用唯一`AttachmentPreview`播放链路；音频、其他附件、`定位文件`、受控附件路径和原始文件不受影响。自动验证：前端全量205/205、TypeScript、Vite生产构建、`git diff --check`与v130隔离`--prepare / --verify`通过；EXE 38,838,784 bytes，SHA-256=`F2EB875416B48209C506E1EB837F51E200102D5CD4816459A0B97E48E88CD08B`。Codex未启动v130、不打开或写入正式数据库；真实 WebView2 点击视频`全屏预览`待南烛枫先关闭旧v129窗口后从当前 BAT 验收。

2026-08-12 v131 AI 自动整理整卡入口：必测：① AI 自动整理入口本身是唯一原生按钮，点击图标、标题、说明、右侧方向区和键盘聚焦后按 Enter/Space 均打开同一二级设置。② 不得保留`进入设置`或其他独立CTA；右侧仅显示低存在感的前进箭头，整卡消费`lift`的悬浮、焦点和按压反馈。③ 88px高、900px列宽、18px/16px间距、场景皮肤`rgba + blur`玻璃材质与原版浅色实体表面不变。④ 二级弹窗仍支持遮罩/Escape关闭，模型通道、密钥、保存和用量逻辑不变。自动验证：前端全量205/205、TypeScript、1702×1066浏览器样板确认左侧图标区域点击可进入且 Escape 关闭通过；v131隔离`--prepare / --verify`待生成。Codex不启动应用、不打开或写入正式数据库。

2026-08-12 v132 视频专属全屏预览：必测：① 正文视频原生控件下方保留`全屏预览`，点击进入唯一`AttachmentPreview`。② 音频不得生成“打开大预览”按钮、标题或点击路径，仅保留原生`<audio controls>`与通用`定位文件`。③ 音频/视频受控存储、原始文件、文件定位和视频最高层预览均不变。自动验证：前端全量合同、TypeScript与v132隔离`--prepare / --verify`待生成。Codex不启动应用、不打开或写入正式数据库。

2026-08-12 v134 模型卡片选择与视频专属预览：必测：① 模型触发器不得使用原生`select`，而应显示当前供应商/型号、同色竖条和文字能力定位；展开后为可滚动的`listbox`卡片。② OpenAI/Anthropic/DeepSeek/Qwen的色条与浅色卡底固定为青绿/暖陶橙/理性蓝/低饱和紫；未知提供商为中性蓝灰，能力信息始终以文字表达。③ 选择任一模型只更新本地待保存选择并关闭面板，保存、通道、API Key、用量、Escape/遮罩关闭不变；没有模型目录时显示诚实空态。④ 正文音频仅原生播放和定位，正文视频保留全屏预览。自动验证：前端全量205/205、TypeScript、1702×1066浏览器样板确认整卡设置入口和模型选择面板可打开、`git diff --check`及v134隔离`--prepare / --verify`通过；EXE 38,838,272 bytes，SHA-256=`EFFAC2C1FA1E923DEBB8838B7B705F00CDDD156DE2694739948129C4B38710C4`。Codex未启动应用、不打开或写入正式数据库；真实WebView2下的非空模型目录卡片观感待南烛枫确认。

2026-08-12 v135 大尺寸 AI 设置与共享模型选择：必测：① 1702×1066下 AI 自动整理二级设置默认约`1366×872`，旧`v1`尺寸偏好不得覆盖，并继续允许用户手动调节。② “选择 AI 模型”打开第二层独立`dialog`，主设置仍在后方保持打开；模型窗默认`840×760`，仅其长模型列表滚动。③ 供应商色条、型号、能力文本、选中态、遮罩点击关闭与 Escape 行为由唯一组件提供；第一次 Escape 仅关闭模型窗，第二次才关闭主设置。④ 主题洞察与主题管理都不得再保留原生模型`select`，必须复用这一组件并展示相同的跨供应商区分。自动验证：前端206/206、TypeScript、Vite生产构建、1702×1066主设置分层对话框 Playwright样板和主题管理四模型样板、`git diff --check`及v135隔离`--prepare / --verify`均通过；EXE 38,840,320 bytes，SHA-256=`43D37C8D62A159871F99E0E6D1DE3EEDA7404F2BA427413CBC88D2BAF4FA770A`。Codex未启动应用、不打开或写入正式数据库、不调用真实AI；真实 WebView2 下的非空模型目录卡片观感与选择保存待南烛枫确认。

2026-08-12 v136 模型选择布局回归修复：必测：① 两个模型时，每个模型卡只能是64–88px的紧凑内容行，模型弹窗随内容收缩；不得把选项拉伸填满760px。② 长目录才允许模型列表滚动，最大列表区为560px。③ 主题管理模型触发器独占首行，宽度大于180px、高度不超过48px；`全部 / 待处理`只能在其下方双列，不得再压缩模型文字为竖排。④ 设置、主题洞察和主题管理继续共享同一选择组件、色条和关闭行为。自动验证：TypeScript、前端206/206、Vite生产构建、1702×1066主题管理四模型实测（卡高68px、触发器单行）、`git diff --check`与v136隔离`--prepare / --verify`均通过；EXE 38,840,320 bytes，SHA-256=`5F34C2A653561BB924002A8C3E86B14DBA1676E3DF428537759ECEFC49239E45`。Codex未启动应用、不打开或写入正式数据库、不调用真实AI；真实WebView2由南烛枫按当前BAT确认。

2026-08-12 v137 主题管理工具栏密度：必测：① 搜索、模型选择、`全部 / 待处理`固定36px，模型文字保持单行；② AI 两个主动作固定38px，既不笨重也不压缩图标/文字；③ 模型标签没有额外容器边框，只有实际选择触发器有边框，窄栏隐藏能力副标题，独立弹窗保留完整信息。自动验证：TypeScript、前端206/206、Vite生产构建、1702×1066主题管理浏览器样板（搜索/模型均36px）、`git diff --check`和v137隔离`--prepare / --verify`均通过；EXE 38,840,320 bytes，SHA-256=`E053E7F2396825EB457E9E938C5679D8E0F4B221D1126CB5F2BC929B0DCE6DC8`。Codex未启动应用、不打开或写入正式数据库、不调用真实AI；真实WebView2由南烛枫按当前BAT确认。

2026-08-12 当前验收 BAT 防误报：根目录 BAT 只能检查固定 v137 EXE 并用可见 `start` 启动，禁止包含`ExecutionPolicy Bypass`、隐藏 PowerShell 窗口、构建、环境变量删除或其他维护动作；`--path`只输出目标路径且不启动程序。隔离构建与SHA校验保留在 PowerShell 维护脚本的显式`-Prepare / -Verify`入口。长期对外分发必须使用可信代码签名的安装包；未签名本地开发 EXE/BAT 仍可能被个别安全产品误报。

2026-08-12 v138 千问直连与任务模型路由：必测：① 设置二级弹窗展示`千问直连`，未填 Key 也可见三张 Qwen 模型卡；Key 只经 Windows 凭据库保存和按用户点击短暂显示。② 全库整理选择千问时，逐篇语义档案与批量归属请求必须为`qwen3.7-flash`，跨文档结构与主题整合必须为`qwen3.7-plus`；任务阶段模型及该批用量均保存，增量不得跨模型复用档案。③ 单主题洞察默认`qwen3.7-plus`；只有明确选择`qwen3.8-max-preview`或`deepseek-v4-pro`才使用高难档，不可自动跨通道回退。④ 千问走阿里云百炼 OpenAI 兼容`chat/completions`，真实调用时清晰报告 Key、地区或模型权限错误。自动验证：TypeScript、前端定向 Qwen 合同、Rust AI 11/11、Vite、`git diff --check`；前端全量仍有1项既有Explorer源码字符串断言失败（不涉及AI）。真实API、正式数据库和真实WebView2由南烛枫从当前BAT验收。

2026-08-12 v140 全通道固定任务路由：必测：① 设置页“任务路由基准”只确定通道/模型家族，主题管理不出现临时选择；全库任务创建后将逐篇档案模型和跨文档整合模型同时写入检查点，续跑不得随设置或目录刷新改变。② 千问固定`qwen3.7-flash → qwen3.7-plus`，仅明确选择`qwen3.8-max-preview`时主题洞察使用高难档。③ DeepSeek 固定`deepseek-v4-flash → deepseek-v4-pro`，Pro 承担跨文档整合及主题洞察，不自动访问未核对的更高档。④ OpenRouter 只在当前选定供应商家族内，按 Flash/Mini/Haiku/Fast/Luna/Small 选逐篇档，按 Terra/Sonnet/Plus/Pro/Medium 选跨文档档；Sol/Opus/Max/Ultra 仅在明确选择时用于主题洞察；缺档保守沿用基准，绝不跨供应商。⑤ 任务阶段与各批用量继续记录在账本中，增量只复用同一实际逐篇模型的同正文档案。自动验证：前端208/208、TypeScript、Rust119/119（另1项真实Shell测试显式忽略）、`git diff --check`、v140隔离`--prepare / --verify`通过；EXE 38,901,760 bytes，SHA-256=`CD35BE08F2311C46F2B2814167999512EBF459D3094C87649298FBE762492BFC`。正式 SQLite、真实 API 与真实 WebView2仍待南烛枫从当前 BAT 验收。

2026-08-12 Windows代码签名门槛：`scripts/sign-windows-artifact.ps1 -Verify -InputFile <EXE>`必须返回`signtool verify /pa /all`和PowerShell Authenticode状态`Valid`。实际签名必须显式提供带私钥、代码签名用途的受信任证书指纹与HTTPS时间戳URL；拒绝自签名证书。当前v137 EXE为`NotSigned`，未执行签名，不能把透明BAT当作安全产品信誉的替代品。

## 当前验证边界

当前代码已经接入正式知识 UI、Tauri 命令、Rust/SQLite 知识仓库和 migration v7 AI 派生对象；历史 migration v3–v6、隔离副本迁移、完整恢复、失败回滚、隐藏命令桥和可见 BAT 用户路径均保留原层级证据。当前正式`D:\南枫知识库`未由本轮 Codex 打开或应用 migration v7。以下内容仍不能标记为完成：

- 拆分提交（首版明确不提供，除非用户重新确认产品边界）；
- 外部图片/链接在用户实际默认应用中直接前台显示的最终 BAT 确认；
- GitHub Release。

v103 当前自动证据：前端187/187、TypeScript、Vite生产构建、Sites4/4、Rust103/103、Release `cargo check`及1702×1066 AI-only核心E2E 1/1通过。唯一BAT完成`--prepare / --verify`，隔离EXE为38,254,592 bytes，SHA-256=`26C0AFBBAAE169B3A64B6ACA00762CF72C380761CAD892D00AC45222208CD101`。未启动程序、未打开正式数据、未调用真实AI。

v104 新增四知识面板切换视口稳定合同：将Tab固定到右侧滚动视口顶部，连续切换四态后逐次检查Tab条、右卡顶部和`window.scrollY`；位置误差必须≤1px。失败样本修复前下跳126px，修复后1702×1066核心E2E 1/1通过。v104唯一BAT完成`--prepare / --verify`，隔离EXE为38,254,592 bytes，SHA-256=`819E15815DFCAA48B7EA0E40E5340BA52D466C2F06938B075F0C95393C05F276`。正式WebView2物理点击未执行。

v105 新增所有权与横排合同：taxonomy AI 必须在全部归属确定后生成每主题整合；主题管理卡三与主题洞察读取同一已应用 revision，待审核草稿不得遮蔽正式结果；topic insight AI 的竞争假设、判断演变和决策与行动分别进入对应面板。归属说明段落必须实算为`writing-mode: horizontal-tb`、宽度大于180px且高度小于80px。前端189/189、Sites4/4、TypeScript、Vite、Rust103/103、1702×1066核心E2E 1/1及v105唯一BAT `--prepare / --verify`通过；隔离EXE为38,234,624 bytes，SHA-256=`A12F2CE6F3E9C999BFF57E309AD0552D96219027972A0BB11AC78DDD127D5421`。真实AI、正式数据库和真实WebView2物理点击未执行。

v106 新增固定分屏与完整阅读合同：卡三必须实算为上下内容区等高，四知识Tab固定在中间，只有下半区`overflow-y:auto`；连续切换四态时上半区、Tab、下半区及窗口坐标误差≤1px。上半区不得再使用`details`折叠AI成果，`查看全部`弹窗复用同一完整内容组件并验证Escape、遮罩、滚动锁定和焦点恢复；最深独立AI阅读卡悬停只改变自身位移。主题管理卡三必须按已应用revision的`sourceItemIds`展示主题整合自动关联的笔记来源。前端189/189、Sites4/4、TypeScript、Vite、Rust103/103、完整Playwright18/18及v106唯一BAT `--prepare / --verify`通过；隔离EXE为38,236,160 bytes，SHA-256=`451770FB812832676DC21CEC96F4EF675814441A236DCBD7CB17B68AECD3BB9D`。真实AI、正式数据库和真实WebView2物理点击未执行。

v107 新增 AI 结果所有权、反馈和材质边界合同：主题洞察实算为上方1/3、Tab、下方2/3，只有下方滚动；完整洞察弹窗默认约80%视口并可调宽高，所有桌面弹窗默认支持人工调整。竞争假设标题不大于14px，来源链接不大于正文且不得溢出；AI`openQuestions`和假设证伪条件进入待验证问题/知识有效性，判断演变提示允许时间顺序、更新或前后证据差异生成来源绑定节点。主题管理没有已应用revision时卡二/卡三为空，不得出现旧归属和`已归入当前主题`；全库分类和单主题整理均验证持续过程弹窗与需确认结果弹窗。设置页/回收站一级卡实算为rgba玻璃背景和blur，内容前景卡继续不透明；AI设置主卡16px圆角，当前程序位于设置末尾。前端189/189、Sites4/4、TypeScript、Vite、Rust103/103、完整Playwright18/18和截图刷新后的定向核心E2E1/1通过；v107唯一BAT完成`--prepare / --verify`，隔离EXE为38,237,696 bytes，SHA-256=`45D93FCF64346BD0BC20485B1255109FF32C4C5B0DFA8D69C052882444466931`。真实AI、正式数据库和真实WebView2物理点击未执行。

v108 新增加载环几何合同：`.save-spinner`必须等宽等高并保持`border-radius:50%`；弹窗头部圆角方形语义图标只允许匹配首个直接子`span`，不得覆盖右侧加载环。单主题AI整理、AI全库分类、批量任务列表和页面/搜索/导入加载均复用同一圆形环。前端190/190、1702×1066定向核心E2E1/1、TypeScript、Vite及v108唯一BAT `--prepare / --verify`通过；隔离EXE为38,237,696 bytes，SHA-256=`1D23B5405BED6A6DD08A8A83E4BD006BBFAB12B4953427754603C4089893E2F8`。真实AI、正式数据库和真实WebView2物理点击未执行。

## 自动回归门槛

每次 checkpoint 至少运行：

1. `npm test -- --run`：前端领域、导入内容、Markdown 和仓库合同。
2. `npm run test:rust`：SQLite migration、知识仓库、导入、附件、备份恢复和安全边界。
3. `npm run test:sites`：静态部署回退合同。
4. `npm run typecheck`：TypeScript 边界。
5. `npm run build`：Vite 生产构建与 Sites 产物。
6. `npm run test:e2e`：无界面核心流程和正式知识入口。
7. `git diff --check`：空白错误。

自动回归只能证明合同与无界面交互通过，不能替代真实桌面或正式数据验证。

## 2026-08-12 v121 回收站反复活回归

1. 来源侧车创建的 Record 移入回收站后，再次请求同一来源操作必须返回“请先恢复”，不得回写活动列表或新建 Record。
2. 同一来源必须同时从全部笔记、主题详情、主题来源计数、原文读取、附件时间线与 AI 全库分类输入中隐藏；显式恢复后才重新可见。
3. 前端在来源列表删除成功后必须从当前列表、搜索结果和轻量会话快照同步移除，避免残留卡片成为再次操作入口。

## 2026-07-29 来源与主题交互修复验收

本轮新增的最小回归边界：

1. Legacy Record 移入回收站后，对应 Source Item 从全部笔记查询隐藏；恢复后重新显示。
2. 来源详情首层只保留“查看知识成果”和三点按钮；收藏、持续跟踪、MD/DOCX、完整正文和软删除在菜单内。
3. 三点菜单点击空白区域或按 Escape 关闭；应用头部与普通记录列表继续遵守同一规则。
4. 来源列表“顶部 / 滑块 / 当前”只滚动列表，不改变选中来源；正文文字功能称“搜索”。
5. 来源搜索框独占一行，不与筛选按钮挤在同一行。
6. 主题管理的新建、边界、别名、规则和关系建议分别进入对应真实操作。

已通过：TypeScript、前端领域测试 87/87、Rust knowledge 26/26、Vite、Windows Tauri `--no-bundle`、新隔离目标构建和 BAT `--verify`。

尚未执行：本轮 Playwright 交互/同视口截图和南烛枫真实桌面；不能使用上一轮截图或旧 BAT 代替。

## 正式知识入口合同

1. 默认应用入口显示`主题洞察 / 全部笔记 / 主题管理`；旧收录箱并入全部笔记，旧主题浏览器与整理工作台并入主题管理，不再暴露假数据原型路由。
2. 浏览器无 Tauri 桥接时显示诚实空状态，不注入业务假数据。
3. 全部笔记显示真实 Source Item、原文、自动归类结果、笔记数和主题归属；无正文空会话默认隐藏但保留数据，无充分证据时不得展示 0 分候选。
4. 主题管理 AI 修订必须完整覆盖全部有效笔记，每篇恰好一个主主题；低置信或多义项标记待核对，不再测试本地分数阈值。
4a. 每个有归属笔记的主题必须同时生成非空主题整合，并把该主题全部归属笔记完整写入`sourceItemIds`；遗漏、重复、跨主题引用、空正文或空来源均不得保存或应用。主题管理显示整合覆盖数，主题洞察只读取同一已应用整合与来源清单。
5. 主题洞察默认读取竞争假设，并在同一阅读区切换判断演变、主题整合和决策账本；主题整合只显示一个成果。关联笔记/来源的卡片主体只在当前页切换正文，最右侧外链图标才进入全部笔记，整卡不得跳转；原始笔记/来源继续只承担输入、正文切换与回溯。四个面板进入笔记后，`返回上一级`必须回到原面板并定位原来源；直接进入全部笔记时默认回到竞争假设的对应来源。任何方式打开或切换笔记都必须清空旧正文查询/高亮并从正文顶部开始；只有本次手动提交正文搜索才允许滚动。正文历史点击只回填输入，不自动搜索。
5a. 侧栏不得显示`判断更新`人工入口；`updated`历史记录状态只作为兼容数据和共享筛选值保留。新导入只进入待 AI 分类；下一次主题管理全库修订统一覆盖，不再启动本地自动整理队列或按算法版本续接。
6. 主题管理读取真实领域/主题边界、自动归类依据、层级关系、别名和待处理事项；维护入口为次级操作。零来源主题保持在列表中，但不得仅因零来源进入待处理筛选、生成检查任务或要求用户逐个判断。
   - 顶层叶子主题不得显示由“领域直属 / 暂无子主题 / 暂无关系 / 0个对象”组成的占位区；存在真实父主题或子主题时，`主题层级`只列实际内容。
7. 合并必须先预览影响，再事务提交，并可撤销。
8. 同时拥有当前判断的两个主题不得直接合并，必须先由用户处理冲突。
9. 拆分只提供只读方案，不显示伪提交入口。
10. 关系建议只生成确定性候选，人工确认后才写入。
11. 个人目录预览不写库，明确确认后增量、幂等写入且不覆盖已有主题。
12. 分类规则、主题别名和实体词典 CRUD 由 Rust/SQLite 持有；分类纠正写反馈并可撤销。
13. Note 与 Source Item 分离；Note 更新和归档不能覆盖原始来源正文。
14. Proposition 是独立对象；Turning Point 只由用户明确选择前后判断后创建，变化原因本身不能自动升格。
15. Evidence 锚点按来源类型校验；可信度、验证状态和有效状态分开持久化。
16. 浏览器桥接用例从最终主题洞察读取 Topic、Judgment、Source 等正式对象，确认旧`维护与录入`CRUD 不恢复为首屏路径；写入命令的字段与持久化合同由 Repository/领域测试覆盖。
17. 隐藏真实 Tauri/WebView2 在隔离根写入新知识对象，强制重启后按原 ID 读回，并以只读检查器确认 migration、计数、完整性和外键。
18. 新导入成功后只整理实际新增的 Source Item；知识工作区通过同一仓库入口幂等应用个人目录并停用旧版宽泛托管规则；首次加载自动补算全部旧来源并按当前版本完成标记断点续算。全量升级与新导入统一为：最高候选严格 `>65` 自动确认，`45–65` 仅保存候选。
19. Domain/Topic 重命名和描述编辑保留 ID 与已有关系。
20. 当前判断、TXT、Markdown 和平台会话使用统一 Markdown/角色卡阅读组件。
21. 快捷键弹窗使用独立尺寸键和内容自适应布局，不得继承其他通用弹窗的历史宽高；六项快捷键在桌面宽度下使用两列卡片，在小于760px时转为单列，1280×720与1702×1066均不得裁切或偏离视口中心。
22. 所有次级维护层使用同一“固定控制区 + 独立正文滚动区”合同：返回/关闭控制不得覆盖滚动内容；主题管理`返回上一级`右侧至少留38px，正文顶部裁切边界必须位于按钮下方至少8px，长内容滚动前后按钮与裁切边界坐标变化均小于1px。正常文档流中的设置、导入返回按钮不套用浮层规则。
23. 顶栏动作必须至少具备明确点击结果、可读状态或可访问说明之一；全部笔记只保留`自动整理待归类来源`真实动作，不得再出现直属、无标签、无行为的托盘SVG。已有同等能力的筛选或按钮不得用孤立图标重复表达。
24. 来源正文区域只保留唯一`来源正文`标题、搜索与查看详情；长正文截断属于内部性能策略，不得再显示`正文预览`等重复、无状态价值的标签。
25. 完整笔记导出弹窗默认占视口约90%，上限为1460×980；旧版弹窗尺寸偏好不得覆盖新默认值。弹窗标题、导出按钮和底部格式说明固定可见，只有正文区独立滚动；1702×1066与1280×720均断言底部说明位于弹窗可视范围内、弹窗外层无滚动。
26. 来源详情标题动作组在1702×1066下保持右对齐并下移到标题行：动作组距标题区顶边至少22px，与标题顶边偏差不超过3px；前两个动作按钮高29–31px、文字12px、图标13px、间距6px。导航与查看详情仍可见可用，小窗口不得与标题和元数据重叠。
27. 图片与视频统一使用视觉媒体视口：默认尺寸为当前应用可用区域，图片按`naturalWidth × naturalHeight`建立原始像素渲染面且不强制放大小图，视频按真实宽高完整等比适配；二者外壳、标题栏和画布统一暗底，视频元素纯黑承托，PDF与文本保持阅读浅色。预览窗口只由CSS原生`resize`调整；视觉媒体缩放和平移不拥有窗口角落。`Ctrl + 滚轮`按光标缩放，普通滚轮不得缩放；桌面中键和标题栏`适合屏幕`按钮都必须恢复当前窗口完整适配、归零、居中。视频适配态保留原生播放/控制语义，缩放后仅由独立拖动手柄平移，不使用固定像素猜测原生控制条范围。
28. 主题结构次级维护默认只显示顶部`编辑`，不得同时出现顶部`添加领域 / 添加主题`或行内编辑表单。进入编辑态后，每个领域行显示`添加主题 / 编辑领域`，每个主题行显示`添加子主题 / 编辑主题`，领域列表末尾显示`添加领域`；四类入口必须打开同一个结构编辑弹窗并带正确领域/父主题上下文。弹窗支持`Escape`与点击遮罩关闭。1702×1066检查完整交互，1280×720检查领域标题、主题标题、来源数和操作按钮不重叠且容器无横向溢出。
29. `设置 → 数据与存储`固定为三层唯一入口：数据交换页不得出现完整备份创建；完整备份创建与恢复必须成对显示；高级维护默认折叠，展开后只显示数据目录、完整性检查、索引重建、数据库快照创建/恢复。数据库快照必须明确不含附件、导入原件和界面设置。静态合同断言完整备份只由设置页消费，1702×1066检查默认折叠、展开布局与导出页去重；自动验证不得点击真实备份或恢复按钮。

## 性能合同

1. 全部笔记首屏最多读取 120 条轻量摘要，不在列表命令中传输完整 `original_text`。
2. 只有选中来源后才按需读取正文；切换来源时不得闪现上一条正文。
3. 超过 20,000 字的正文默认显示流畅预览，正文框内不叠加遮挡信息；“查看详情”以全部笔记同款大弹窗显示完整正文。
4. 旧来源详情、版本和附件只在对应来源页读取，全部笔记列表不预取。
5. 桌面宽屏仅左侧来源列表滚动；页头和右侧阅读/分类卡不得随左侧移动。
6. 正文预览占用主题归属之外的剩余高度；主题归属过长时只滚动自身。
7. 数据库、文件、附件、导出和备份类 Tauri 命令不得同步占用 UI 主线程。
8. 同一时刻相同 Repository 读取必须合并；来源正文缓存必须有固定上限。
9. scroll、resize 和 ResizeObserver 触发的关联线测量每动画帧最多执行一次，亚像素差异不得触发 React 状态更新。
10. 200 篇、每篇约 2 万中文字符的全部笔记摘要读取必须在 500 ms 合同内完成；1000 条记录摘要性能合同继续保留。
11. 搜索输入先反馈，列表筛选与长正文解析允许延后；不得用删除功能、减少正式数据或降低图片质量换取流畅度。
12. Playwright 必须运行生产构建预览；不得把 Vite 开发服务器首次即时编译耗时当作成品页面交互延迟。当前全量 E2E 为 14/14 通过。

## migration v3 合同

- 迁移前创建 SQLite 安全备份。
- 旧 Record 保留为全部笔记兼容层，不就地扩成所有新对象。
- 每条活动 legacy Record 幂等映射为一个 Source Item；重复执行不增加数量。
- 未确认来源保留在全部笔记待确认状态，不自动把标题升格为 Topic。
- 知识表、FTS、外键和唯一约束创建成功。
- 迁移后 `PRAGMA integrity_check=ok` 且外键违规为 0。
- 隔离验证工具必须要求专用标记，并拒绝 `D:\南枫知识库` 和 `D:\南枫情报台`。
- 正式升级前必须先完成完整迁移备份恢复演练；失败时保持旧数据可恢复。

## 分类合同

固定权重：

- 显式规则 35；
- 别名与实体 25；
- FTS5/BM25 20；
- 集合相似 10；
- 时间历史 10。

固定阈值：

- `65.01`：最高候选具备自动接受资格，仍必须记录操作并可撤销；
- `65`：候选列表，不得自动接受；
- `45`：候选列表；
- `<45`：人工处理或建议新建主题。

每条验收数据至少检查：原件先归档、提取完成后分类、相同输入可重现、分数与理由可解释、失败不影响其他队列项、无网络和模型 API 仍可运行。

分类 v5 固定回归样本至少覆盖：

- A股交易新规 → `A股与基金市场`，不得出现车型、转写或模型成本；
- 理想 i8 二手选择 → `新能源车与二手车`；
- A 型血输血 → `血型与输血`；
- 艺术专业报考 → `艺术与设计升学`；
- giffgaff Wi-Fi Calling → `海外通信与号码`；
- 美国银行卡方案 → `海外银行与支付`，不得因原始 JSON 中的 `Claude`、`Google`、`SRT`、`ACES` 或 `GPU` 生成候选；
- 日本楼市跌幅与分化 → `房地产与楼市研究`，不得因地点不同误塞入南京租房；
- 南枫批量改名设计 → `品牌、图标与界面设计`；
- GitHub 分支保护建议 → `Git 与版本控制`，不得只因出现 Codex/GitHub 归入 Agent；
- 账本恢复失败原因 → `数据备份、迁移与恢复`；
- 无消息、无名称、无摘要的会话 JSON 不进入默认收录箱列表，同时数据库记录仍保留。
- `Untitled`、单字或错误标题只要用户可见正文有明确主体，仍须由正文生成候选。
- 无候选来源必须保存当前版本完成标记；再次启动只处理未完成来源。

目录覆盖回归还必须验证：

- 托管主题名和跨主题短语不重复；
- `ChatGPT`、`Claude`、`Google`、`Apple`、`SRT`、`ACES`、`GPU`、`价格`、`视频`、`动画`、`设计`、`账号`、`GitHub`、`银行`、`汽车`、`投资` 等宽泛单词不能成为独立托管规则；
- 标题中的明确主题短语与正文命中走同一内容规则；
- FTS5/BM25 的命中短语必须同时存在于用户可见投影，原始 JSON 元数据不得单独提供全文分。

全量隔离覆盖审计还必须验证：

- 901 条来源全部进入同一分类器，不得只测试截图样本或首屏 120 条；
- 清晰正文无候选必须逐条复核，最终无候选只能是空记录、极短占位或没有业务主体的请求；
- fresh 隔离副本只应用当前目录版本，避免多轮调试残留规则污染结果；
- 上下文导出使用 `query_only`，导出前后写入计数和数据库哈希一致；
- 证据见 `docs/audits/2026-07-27-classification-v5-full-coverage/summary.md`。

## 数据与安全合同

- 远程 Markdown 图片默认不自动联网。
- 附件打开、删除和恢复只允许 canonical 受控路径。
- 完整迁移备份逐文件记录 SHA-256；旧版无哈希备份只允许预览。
- 恢复前校验文件集合、大小、哈希、数据库完整性和可用空间。
- 备份与恢复在后台工作线程运行，不阻塞 WebView。
- ChatGPT 完整 ZIP 按“消息 → 附件 ID → `.dat` 实体”映射，签名优先恢复格式，未知格式不猜测。
- 真实标题、正文、附件路径和原始导出包不进入 Git 审计摘要。

## 隔离数据验收基线

当前已验证的 901 条隔离副本基线：

| 项目 | 期望 |
|---|---:|
| 活动 Record | 901 |
| Source Item | 901 |
| 收录箱 | 901 |
| 附件登记 | 446 |
| migration | 1、2、3 |
| integrity_check | ok |
| 外键违规 | 0 |
| 第二次迁移 | 数量不变、无重复备份 |

证据见 `docs/audits/2026-07-27-knowledge-production-integration/summary.md`。该基线仅证明隔离副本迁移，不证明正式数据已升级。

## 可见 BAT 验收基线

2026-07-27 已通过 BAT 重建与普通启动验收。知识工作区、长正文、收藏取消后恢复、PNG/WAV 附件打开入口和两次普通关闭均通过；正式库回查保持 migration 1/2/3、901 Record、901 Source Item、901 收录箱、446 附件、`integrity_check=ok` 和外键 0。

正式数据根的批量导入页、多文件选择器和取消路径通过；实际批量预览与确认在隔离数据根完成，JSON 与 Markdown 两个脱敏文件共导入 3 条，跳过 0、失败 0，隔离库完整性和外键通过。证据见 `docs/audits/2026-07-27-visible-bat-acceptance/summary.md`。

性能优化版 BAT 已重新生成。可见检查确认正式库首屏为 120 条轻量摘要、长正文默认 20,000 字预览、展开/恢复按钮可用，切换记录先进入空白/加载态而不显示旧正文。

南烛枫随后确认性能优化后“流畅度好多了”。本轮又在隔离根导入 12 条脱敏记录，成功 12、跳过 0、失败 0；自动整理建立可编辑默认目录，分析 12 条并保留 12 条低于自动阈值的待确认建议。可见窗口确认只有左侧列表滚动，页头和右侧卡保持固定；Playwright 进一步验证两者位移为 0、正文区高度超过 300 px。

外部链接/附件已统一改为 Rust 的 Windows 原生 `ShellExecuteExW + SW_SHOWNORMAL` 路径，并在调用前授权默认应用前台激活；Rust 合同和生产构建通过。自动桌面检查在点击链接后因无法可靠确认浏览器 URL 而按安全规则停止，最终前台行为仍由南烛枫使用最新 BAT 确认。

本轮脱敏证据见 `docs/audits/2026-07-27-reading-auto-organization/summary.md`。

## Windows 安装生命周期基线

2026-07-27 本地 NSIS `0.2.0` 已通过：

1. 全新静默安装、隔离根启动、优雅退出和卸载；
2. `0.1.0` 创建的隔离记录升级后在 `0.2.0` 可见，migration 1/2/3、1 Record、1 Source Item、1 收录箱、完整性和外键通过；
3. 旧版运行中安装器退出码 2，旧版保留且新版未安装；
4. 旧版关闭后升级退出码 0，旧卸载项和旧程序目录被移除，只保留新版；
5. 新版只读打开正式 `D:\南枫知识库` 后，数据库 SHA-256、901 条记录、901 个 Source Item、446 个附件、完整性和外键均不变；
6. 卸载后程序目录和卸载项移除，正式数据及隔离升级数据均保留。

证据见 `docs/audits/2026-07-27-windows-installer-lifecycle/summary.md`。

## 发布前人工门槛

已关闭的隔离门槛：

1. 新隔离根的完整迁移备份创建、预览、恢复、故障注入和自动回滚；
2. 隐藏 Tauri/WebView2 的正式命令桥、migration v3 和强制进程重启后的持久化；
3. 901 Record、901 Source Item、446 附件登记、界面偏好、逐文件 SHA-256、完整性和外键核对。

已关闭的正式门槛：

1. 正式 migration v3、完整备份、第二次打开幂等、完整性和外键复核；
2. BAT 重建、普通启动、知识工作区、长正文、收藏恢复、附件打开入口、隔离批量导入和优雅退出。

仍待确认或授权：

1. 南烛枫使用最新 BAT 确认图片和外部链接直接在前台显示，并复核独立滚动、统一正文和自动分类体验；
2. 发布 GitHub 需再次明确“上传”授权并执行正式 Release 验证。

## 2026-08-03：固定筛选顶角裁切与滚动遮蔽 v68

- 定向合同：固定层仍位于真实列表滚动层内并保持`sticky/top:0`；固定层与滚动视口的两个顶部圆角均为14px；滚动视口保持`overflow-y:auto`。
- 材质合同：珍珠雾面与14px底部渐退提高遮蔽，滚过文字只留不可辨认色影；搜索/筛选前景继续更亮；不增加滚动状态属性、不恢复嵌套`backdrop-filter`。
- 自动结果：前端205/205、TypeScript、Vite、1702×1066四皮肤E2E 1/1及1280×720顶部圆角边界实算通过。
- Windows隔离结果：v68 `--prepare / --verify`通过，EXE 34,867,200 bytes，SHA-256=`FE6BB653AB16BFE83B498B7408E993A7E518DE25D549CE9EEB655DEE280AB1FF`；未启动应用、未打开正式数据。

## 2026-08-03：固定筛选合成层裁切与整行遮蔽 v70

- 失效证据：南烛枫真实WebView2截图确认v68仍在右上暴露方形白槽，底部`ChatGPT导入`仍清晰；v68的“通过”仅代表旧静态/Chrome合同通过，不能继续作为真实桌面通过证据。
- 失败合同：先要求出现非滚动`.knowledge-source-list-viewport`，由其持有14px圆角、`overflow:hidden`和`isolation:isolate`；要求固定承托面覆盖滚动条槽，要求遮蔽至少覆盖一整组标题/辅助信息。修改前该定向合同1失败、27通过。
- 当前静态合同：外层裁切、内层`height:100% + overflow-y:auto`、固定承托面右延伸12px、52px遮蔽和前84%高不透明度均必须存在；禁止在固定层恢复`backdrop-filter`。
- Chrome实算E2E：1702×1066与1280×720验证固定层仍为`sticky`且滚动位移0；外层为14px/hidden/isolate；固定层与裁切视口右边缘差≤0.5px；内层圆角为0，证明不再依赖滚动元素裁切。测试主动把真实标题置于固定层底部12px处并截图，整组标题/辅助信息不可辨认。
- 视觉证据：`.runtime-qa/knowledge-final-layout-evidence/source-fixed-frosted-overlap-1702x1066.png`；只证明当前源码Chrome渲染，不冒充WebView2。
- Windows隔离结果：v70 `--prepare / --verify`通过；路径`.runtime-qa/fixed-filter-clip-v70-app/release/nanfeng-knowledge-base.exe`，34,866,688 bytes，SHA-256=`7BFBB5D79330BE3D9176171B2A65C6CE8E51D5914FD663E1BAFAC0826A0EE15B`；未启动应用、未打开正式数据。

## 2026-08-03：全部笔记标题动作首帧稳定性 v71

- 复现：直接打开`全部笔记`时，标题区先渲染自动整理，随后主题详情和正文异步返回后再插入`返回上一级 / 查看详情`，动作组数量与宽度发生变化。
- 失败合同：要求返回目标由首帧已有的`selected.primaryTopicId`兜底，要求查看详情不再被`selectedOriginalText`条件包裹；修改前定向静态合同1失败、28通过。
- E2E延迟合同：浏览器夹具将`get_knowledge_source_original_text`与`get_knowledge_topic_detail`各延迟1.2秒；标题出现后立即读取按钮，必须已按`自动整理 / 返回上一级 / 查看详情`稳定呈现，延迟结束后的文字、数量和动作组矩形必须完全相同。
- 保持项：按钮顺序、样式、自动整理禁用语义、真实返回上下文和详情正文内容不变；没有添加延迟动画、强制刷新或伪造数据。
- Windows隔离结果：v71 `--prepare / --verify`通过；路径`.runtime-qa/stable-source-actions-v71-app/release/nanfeng-knowledge-base.exe`，34,867,200 bytes，SHA-256=`CAAFD33A74B6322FF1E219AAD3F705DBA99CF2046555AC2BB8AE776698359E83`；未启动应用、未打开正式数据。
- 待验证：南烛枫在真实WebView2中连续上滑，确认两个顶角不再露出直角白卡，固定层下方文字不可辨认且没有纵向亮块。

## 2026-08-03：自动草案完整语义与状态 v72

- 复现：正文包含`的话，我可以单独就“数据中心真实净回报率”帮你拆解。`时，旧生成器因命中`可以`把聊天收尾当成决策；标题又截取前24字符并补省略号，最终显示为无头无尾的`自动草案`。
- 失败后转绿合同：同一真实问题样本不得进入假设、判断、问题、决策、依据或风险；有效建议必须保持完整原句；标题必须为`《来源》中的待确认建议`且不含固定字符截断省略号；行动、实际结果和复盘保持空或未执行。
- 呈现合同：自动卡明确标记`自动决策草案`，按`形成依据 / 待确认建议 / 行动 / 预期与结果`显示；预期需要补指标与复核时间，实际结果明确尚未产生。
- 自动结果：前端210/210、TypeScript、Vite生产构建、1702×1066知识最终视图Chrome E2E 1/1通过；Chrome结果不冒充真实WebView2内容质量验收。
- Windows隔离结果：v72 `--prepare / --verify`通过；路径`.runtime-qa/complete-auto-drafts-v72-app/release/nanfeng-knowledge-base.exe`，34,866,688 bytes，SHA-256=`E27D1DDB52C29C60940B9E6443855CD1F6859FFD73325EF9DFB462607CC902D7`；未启动应用、未打开正式数据。

## 2026-08-03：全部笔记复用收藏筛选外壳 v73

- 失效证据：南烛枫真实WebView2截图再次确认v70仍在右上暴露方形白槽，并在定位条下方出现大块矩形白色遮罩；旧Chrome合同不能作为真实桌面通过证据。
- 根因合同：旧`.knowledge-source-filters::after`以52px矩形向下覆盖列表，直接制造底部直角；`margin-right:-12px`把承托层右圆角推出裁切边界。更上层的结构问题是全部笔记把控制区放在滚动层内再用`sticky`模拟固定，而我的收藏让控制区天然位于滚动列表之外。
- 现行结构：`UnifiedNoteListPanel`直接拥有`SearchRow → DisplayToolbar → Toolbar → Locator → list viewport`；滚动容器只包含笔记卡。生产样式不得出现`.knowledge-source-filters`、其伪元素遮罩、`sticky`或负右边距；全部笔记自身的搜索、组合筛选、整理状态、全部加载和定位功能保持。
- 自动结果：前端210/210、TypeScript、Vite、1702×1066定向E2E 1/1通过；E2E另在1280×720实算控制区均在滚动层外、列表从定位条下方开始、外层14px圆角裁切且文档无横向溢出。
- Windows隔离结果：v73 `--prepare / --verify`通过；路径`.runtime-qa/favorite-filter-shell-v73-app/release/nanfeng-knowledge-base.exe`，34,866,176 bytes，SHA-256=`E90C0F1D58657B965038615EA35BEA6A736B2E8032F5E14A8FD45D39209A735D`；未启动应用、未打开正式数据。

## 2026-08-03：全软件紧凑小卡逐卡微交互 v74

- 失效证据：南烛枫用10组真实界面截图确认v65只覆盖少数小卡，旧E2E也只悬浮一张知识摘要卡，不能证明“全软件”或“每张独立”。
- 结构合同：操作卡使用`lift`，纯阅读卡使用`surface-lift`；角色必须直接落在最深层独立小卡，父容器/整组/链路不得消费，DOM不得嵌套抬升角色；纯阅读角色没有`:active`缩放。
- 消费者合同：当前判断、核心解释、提取依据、支持/反对证据、待验证/有效期、判断演变来源卡、决策四阶段、主题内部卡和待处理子项逐卡消费；竞争假设、决策版本、主题内容区保持静止。
- Chrome实算E2E：1702×1066流程1/1通过；悬浮核心解释时该卡计算位移含`-2px`，兄弟证据卡和竞争假设外壳均为`none`；时间线、决策阶段和主题卡全部逐项拥有角色。
- 自动结果：前端210/210、TypeScript、Vite生产构建通过；未增加动画依赖，保持精细指针门控、显式属性过渡与`prefers-reduced-motion`。
- Windows隔离结果：v74 `--prepare / --verify`通过；路径`.runtime-qa/individual-small-card-motion-v74-app/release/nanfeng-knowledge-base.exe`，34,866,688 bytes，SHA-256=`68B0B5AA052F6838E4108661787BC5B7BDC75CEAA77111B2A5EDB1829EB78057`；未启动应用、未打开正式数据。

## 2026-08-08：千条笔记窗口化与 WebView2 滚动稳定性 v76

- 失败复现：真实 WebView2 在来源列表越向下滚动时出现跳洞、右侧空白和侧栏同步失绘；v75 降低单卡 hover 合成后仍可复现，故不得把单卡样式视为根因。
- 自动压力合同：对3,000条、82px行高、640px视口计算窗口范围，实际渲染行数必须少于24；滚动条总高度保持246,000px。定位条首项=0、中间项=40,713px、末项=81,344px，均不得越界。
- 入口回归：全部笔记、普通记录、我的收藏与持续跟踪使用同一窗口化所有者；搜索、筛选、排序、选中、详情关联、定位条和“加载更多”保持原语义。滚轮在真实列表/正文层不拦截，右侧标题区仍按帧转交正文。
- 自动结果：前端211/211、TypeScript、Vite、Sites4/4、Playwright17/17、Rust88/88通过；v76 `--prepare / --verify`通过，隔离EXE SHA-256=`115194EFD0347ECE8F479EA6E20F94BEEAF9AA70B0D45B2E5645F12520DB5799`。未启动程序，未打开或写入正式数据。
- 真实桌面待验：使用`启动南枫知识库-千条笔记滚动稳定性验收.bat`，在全部加载889+条后做顶部→底部→顶部及快速往返滚动；确认三栏无空白/跳洞，并确认右侧标题区滚轮仍滚动正文、定位条首中末项准确。

## 2026-08-08：小卡片悬浮落影与裁切 v77

1. 主题管理：分别悬浮主题边界、自动归类依据、主题层级、别名与术语，以及“待处理事项”空态卡；每张仅自身上移2px并出现自然底部阴影，邻近卡与大阅读外壳不移动。
2. 判断演变：悬浮每一个形成依据、待确认建议、行动、预期/结果卡；阴影必须在四边自然衰减，底部不得出现水平硬切线。
3. 设置 → 数据与存储：分别悬浮存储路径、7张统计卡、数据交换/备份与恢复/优化数据占用三张功能卡，以及高级维护的5个动作卡；每张独立响应，按钮之间不得整组联动。
4. 可访问性：触控或`prefers-reduced-motion`下不移动、不缩放；纯阅读卡无按压缩放，动作卡按下才缩至`.985`。
5. 自动结果：前端211/211、TypeScript、Vite、Playwright17/17与v77 BAT `--prepare / --verify`通过；仅最后由南烛枫双击BAT做真实WebView2视觉确认，Codex未启动应用或打开正式数据。

## 2026-08-09：全入口前景卡统一提亮 v87

1. 静态合同：共享明亮前景Token必须由主题洞察、全部笔记、主题管理、收藏、跟踪、回收站和设置的持久前景卡消费；卡片二与四状态嵌套卡继续消费分层Token。
2. 保真合同：不得修改既有alpha、外层单次模糊、边缘渐退、圆角或布局；原版浅色、浮层/弹窗、大承托层与`主题整合`主要来源阅读卡不进入本轮提亮；红/绿语义卡只提高明度骨架并保留色相。
3. 自动结果：前端218/218、TypeScript和Vite生产构建通过；1702×1066定向运行通过新材质实算并生成`docs/screenshots/qa/foreground-card-brightness-v87-1702x1066.png`。
4. 已知边界：完整历史Playwright随后在既有附件分类弹窗流程被`.prototype-dialog-backdrop`拦截，未声明全量通过；该故障不属于本轮材质改动。
5. Windows隔离：`启动南枫知识库-全入口前景卡片提亮验收.bat --prepare / --verify`通过；EXE为35,435,520 bytes，SHA-256=`87909B402C57BB1F112FA6EF87AFE7405DFCF48C7BA1A33F9334C38883E00A3E`。Codex未启动应用或打开正式数据。
6. 真实桌面：由南烛枫双击该BAT，逐入口检查前景卡是否明亮清楚且大承托层仍有层次。

## 2026-08-10：最前景卡完全不透明 v102

1. 失败证据：`docs/screenshots/final-core-workspace/foreground-cards-translucent-reference-20260810.png`记录真实WebView2中前景白卡继续透入暖色背景并显灰。
2. 静态合同：共享前景、主题树、阅读/嵌套/辅助阅读、AI及绿/红/紫语义卡Token均必须使用不带alpha的`rgb`表面；不得恢复`rgba`半透明前景卡。
3. 消费范围：主题洞察、全部笔记、主题管理、收藏、跟踪、回收站、设置及AI卡内白卡同步；外层承托面、浮层、弹窗、主题整合主要来源阅读卡与原版浅色不进入本轮。
4. 自动结果：前端234/234、Sites4/4、TypeScript、Vite生产构建及1702×1066四皮肤/AI结果态E2E 1/1通过；当前源码截图为`docs/screenshots/qa/foreground-card-opaque-v102-1702x1066.png`与`docs/screenshots/qa/foreground-card-opaque-ai-v102-1702x1066.png`。
5. Windows隔离：根目录当前验收BAT的v102 `--prepare / --verify`通过；EXE为38,298,624 bytes，SHA-256=`FC093F971E1A4D633C909CE902B21C03844B3644DA68E001CCE62F29719235EB`。Codex未启动应用、未打开正式数据或调用真实API。
6. 真实桌面：由南烛枫双击`启动南枫知识库-当前验收.bat`确认最终WebView2观感。

## 2026-08-09：关联笔记完整卡片与独立滚动 v88

1. 布局合同：`.knowledge-final-source-list`的标题和每张来源卡使用内容高度，隐式Grid行不得在固定阅读区内被压缩；`.knowledge-final-source-switch`继续保持至少52px完整卡片高度。
2. 信息合同：每张卡必须同时显示标题与`来源类型 / 日期 / 置信度`，保留主体原位切换和最右侧外链图标两个独立点击目标。
3. 滚动合同：20张来源时`scrollHeight > clientHeight`，右列独立纵向滚动并可将最后一张滚入可视区；主阅读区、三栏比例和页面滚动归属不变。
4. 自动结果：静态合同30/30通过；1702×1066真实组件夹具实算20张卡最小高度不低于52px、全部元数据高度大于0、列表产生溢出并可滚到末项，截图为`docs/screenshots/qa/related-sources-full-cards-v88-1702x1066.png`。
5. 已知边界：完整历史Playwright在上述断言之后仍被既有附件分类弹窗遮罩拦截；未扩修该无关故障，不声明全量E2E通过。
6. Windows隔离：`启动南枫知识库-关联笔记完整卡片滚动验收.bat --prepare / --verify`通过；EXE为35,435,520 bytes，SHA-256=`22715D0CA727300037DCD3515CE20CE8F3804EA4111FAD221A1336C307037BEF`。Codex未启动应用或打开正式数据。
7. 真实桌面：由南烛枫双击该BAT，在20条真实来源主题中确认卡片完整、滚轮/滚动条可到末项且主体与外链动作不变。

## 2026-08-09：附件默认加载与月份筛选 v89

1. 完整目录：构造仅存在正文声明、尚未物化的图片、视频、PDF和TXT；`全部`必须返回4项，图片1项、视频1项、文件2项，所有项在恢复前即出现。
2. 自动加载：当前来源正文与既有附件列表同时就绪后，一次提交全部缺失唯一ID；成功项立即进入原位预览，失败项只标记自身且不阻断其他项，重复加载保持幂等。
3. 原位格式：附件目录和卡片壳立即完整呈现；实际媒体渲染器按接近可视范围有界挂载。图片使用浏览器懒加载，音视频`preload=metadata`并保留原生控制，PDF与文本只在滚动接近时创建阅读器；归档/未知格式不执行。
4. 时间线：全部/图片/视频/音频/文件共享完整目录，按`YYYY年M月`分组，月份和文件数量增加时只增加滚动内容，不隐藏未恢复项。
5. 自动结果：前端219/219、Rust95/95、TypeScript、Vite生产构建通过；Rust定向覆盖恢复前完整目录和批量失败隔离。
6. Windows隔离：`启动南枫知识库-附件默认加载与月份筛选验收.bat --prepare / --verify`通过；EXE为35,557,376 bytes，SHA-256=`9FC494F26A164805879702C339784C35EB10F0E719BD15B93B4FC8782254C9A5`。Codex未启动应用、未打开或写入正式数据。
7. 真实桌面：南烛枫关闭旧窗口后双击BAT；打开含多格式附件的笔记，确认无需点击即自动加载；再分别打开五类时间线，核对完整数量、月份分组、滚动到末项和失败提示。

## 2026-08-09：图片缩略图库与视频代表帧 v90

1. 图片分类：月份组内使用真实缩略图网格，保持完整内容`contain`，点击进入既有最高层图片预览。
2. 视频分类：受控视频在WebView内跳过可能纯黑的第0帧生成代表帧，卡面有播放标记，点击进入既有最高层视频播放器。
3. 自动补齐：分类中可恢复但未物化的媒体按来源顺序调用同一批量接口；单项失败隔离，完成后刷新当前查询，原ZIP和原媒体不改写。
4. 非媒体保真：全部/音频/文件及PDF/TXT继续使用原信息列表；月份分组、独立滚动和搜索不变。
5. 自动结果：前端220/220、TypeScript、Vite生产构建通过。
6. Windows隔离：`启动南枫知识库-图片视频预览图验收.bat --prepare / --verify`通过；EXE为35,558,400 bytes，SHA-256=`9CFC6F379FE8DD0304C7821FC1BB8891AB7656DC4673AD3CC305DC816FEF9D36`。Codex未启动应用、未打开或写入正式数据。
7. 真实桌面：南烛枫关闭旧窗口后双击BAT，核对图片缩略图完整、视频代表帧可辨认、点击可播放，以及尚未物化媒体会在当前分类中自动补齐。

## 2026-08-09：Markdown受控解码与阅读预览 v91

1. 根因合同：Markdown/TXT不得继续使用本地文件iframe；正文原位与最高层预览必须共同消费`AttachmentTextPreview`。
2. 安全读取：命令仅按附件ID读取数据库登记且位于受控附件目录内的文本，大小上限8MB，非文本和目录外路径拒绝。
3. 编码合同：UTF-8中文逐字保留；GBK中文、UTF-8/UTF-16 BOM均解码为Unicode，不修改原文件。
4. Markdown阅读：frontmatter隐藏，标题/列表/表格/引用/代码块按现有`MarkdownContent`渲染；纯文本保留换行并自动折行，整张文档不产生横向滚动。
5. 自动结果：前端221/221、Rust96/96、TypeScript、Vite生产构建通过。
6. Windows隔离：`启动南枫知识库-Markdown阅读预览验收.bat --prepare / --verify`通过；EXE为35,588,096 bytes，SHA-256=`61CB55542E7544A6EC40075D83151DBD2E7E8D969767F8D5CC2CAD7974CD86B0`。Codex未启动应用、未打开或写入正式数据。
7. 真实桌面：南烛枫关闭旧窗口后双击BAT，重新打开失败截图中的Markdown，确认中文、Markdown层级、frontmatter隐藏、纵向滚动和最高层预览一致。

## 2026-08-09：全格式附件定位文件 v92

1. 统一入口：正文原位附件与最高层`AttachmentPreview`共同消费`AttachmentLocateButton`；图片、视频、音频、PDF、Markdown/TXT、归档和其他受控文件均显示`定位文件`。
2. 参数边界：前端只允许调用`reveal_attachment({ attachmentId })`，不得提交`storedPath`或任意本机路径。
3. 后端安全：Rust按附件ID读取数据库记录并复用`controlled_attachment_file`；缺失文件、目录外文件和不存在的附件拒绝定位。
4. Windows行为：通过Explorer`/select`打开附件所在目录并直接选中文件；定位不打开、复制、移动、改名或改写附件。
5. 自动结果：前端222/222、Rust96/96、TypeScript、Vite生产构建通过；目录外恶意附件在既有安全测试中同时验证不能打开、删除或定位。
6. Windows隔离：`启动南枫知识库-附件定位文件验收.bat --prepare / --verify`通过；EXE为35,713,024 bytes，SHA-256=`694284AC80C52CFF72ACF1908A1349B39A164092C7C8D9BE69A087F0EAACB89F`。Codex未启动应用、未打开或写入正式数据。
7. 真实桌面：南烛枫关闭旧窗口后双击BAT，分别在视频、图片、PDF、Markdown/TXT等附件的正文原位操作行和最高层预览点击`定位文件`，确认Explorer打开正确目录且目标文件处于选中状态。

## 2026-08-09：治理收口、媒体交互与有界加载 v93

1. 图片/视频标题栏新增明确`适合屏幕`按钮；中键继续复位。视频适配态保持原生播放与控制，缩放后使用独立拖动手柄，不再以底部64px硬编码判断控制区。
2. 预览窗口只保留CSS原生`resize: both`，删除组件内第二套右下角拖动状态。
3. 正文附件目录完整挂载，但图片、视频、音频、PDF和文本仅在接近可视范围时创建实际预览；视频/音频只预载metadata。
4. 应用右键菜单只在笔记内容区接管；输入框、可编辑区、链接、视频、音频、iframe、设置与导航保留系统菜单。
5. 设置页显示版本、构建标签、当前EXE大小与EXE自身SHA-256；唯一根目录入口为`启动南枫知识库-当前验收.bat`，构建过程使用独占锁。
6. 自动结果：前端222/222、Rust96/96、TypeScript、Vite、Sites4/4、默认Playwright17/17、媒体专项Playwright3/3通过。专项覆盖横屏视频、竖屏视频、150%/200%DPI、原生视频命中与48个PDF目录有界加载。
7. 真实桌面：真实MP4声音、进度条、音量、全屏、Explorer选中、正式Markdown与多格式长笔记仍由南烛枫双击唯一当前BAT确认；Codex不启动程序、不打开正式数据。
2026-08-14 v155 统一任务路由基准：必测：① 设置→AI自动整理仅有一个“任务路由基准”模型框，默认自动规划；三张 API Key 卡不再作为通道切换或另一套路由入口。② 自动模式只在已配置Key中依次选择千问直连（Flash→Plus）→DeepSeek直连（Flash→Pro）→OpenRouter，Max/高难档不自动升级。③ 手动模式将“通道+模型”保存为同一选择；卡片第一行必须明确是千问直连、DeepSeek直连或OpenRouter，第二行仅显示本知识库任务适用场景；未配置来源不可选择。④ 主题洞察批量在开始前读取后端routePreview并冻结实际通道+模型，改设置和目录刷新不得改变在途任务/断点。⑤ API Key 仍只经显式保存与显示命令进入Windows凭据库，设置、路由、账本和UI不返回Key。自动验证：前端220/220、TypeScript、Rust149/149（1项真实Shell按设计忽略）、Vite、Sites4/4、Rust格式/diff通过；v155固定验收`--prepare/--verify`与根BAT校验通过，EXE 29,855,744 bytes，SHA-256=`59C332DA90A387E961637ED362D542C6D51BF5FD83EA9C3AA54F8A0BACAEA730`。全量`knowledge-final-view.spec.ts`被既有暗色皮肤固定RGB断言拦截，未作为本轮失败；正式数据、真实AI与真实WebView2仍未验证。
2026-08-14 v156 API Key输入承托与模型精简：必测：① 已配置Key默认显示遮罩圆点，输入框必须全宽40px，眼睛在框内右侧；不点击眼睛不得把真实Key传至前端。② 无删除Key入口；空输入不得覆盖凭据，只有输入新Key并保存才替换。③ 模型列表只显示千问Flash/Plus/Max、DeepSeek直连Flash/Pro、OpenRouter的OpenAI Terra与Anthropic Sonnet；Luna、Opus、重复Pro和OpenRouter DeepSeek不再显示，旧手动选择被筛除后回到自动规划。④ 任务冻结、缓存与通道来源标签保持v155合同。自动验证：前端220/220、TypeScript、Vite、Rust150/150（1项真实Shell按设计忽略）、格式/diff通过；v156固定验收`--prepare/--verify`与根BAT校验通过，EXE 29,864,960 bytes，SHA-256=`48A8E471BB8C1077A76EC93B3CCC2458912021FCE8A8E82B6D77B75442B9A636`。正式数据、真实AI与真实WebView2仍未验证。
