# 分类 v6 全量准确性审计摘要

> 日期：2026-07-28
> 代码 checkpoint：`4b6b87e`
> 数据边界：正式库只读在线备份；目录协调、旧系统规则清理、上下文导出和分类审计均在 `.runtime-qa` fresh 隔离副本完成

## 结论

- 901 条收录箱来源全部进入 `local-rules-v6` 准确性矩阵，不限于截图样本或首屏数据。
- 886 条生成证据充分候选；15 条为空记录或不足以判断业务主体的极短占位。
- 有有效标题且可见正文不少于 80 字的来源，无候选数量为 0。
- 投资、海外账号、图像生成和汽车等跨领域主体合同未发现冲突标记。
- 正式数据库没有写入 v6 建议，没有执行目录协调，也没有删除正式规则。

## v6 纠偏架构

- 清晰标题只保留标题主体一致候选；无主题标题只用正文开头 600 字判断主体。
- 正文宽泛词、平台实体和历史活跃度只能佐证，不能单独制造主题。
- 缺少标题主体证据的 FTS5/BM25 信号封顶 50%。
- 模糊标题必须同时保持首尾边界，避免仅因通用后缀相同而误归。
- 对公司研究、行业投资、账号风控/身份制度、银行卡维护、会话导出和设备系统功能增加可解释的组合主体边界。
- 托管规则按短语与位置去重；目录中已不存在的 `catalog-rule-%` 旧系统规则会物理删除，用户自建规则不在清理范围。

## fresh 审计证据

隔离根：

`.runtime-qa\classification-v6-fresh-audit-20260728-0210`

| 项目 | 结果 |
|---|---:|
| 来源 | 901 |
| 有候选 | 886 |
| 无候选 | 15 |
| `confirm` | 335 |
| `candidates` | 551 |
| `auto_eligible` | 0 |
| 清晰正文无候选 | 0 |
| 主题 | 68 |
| 有效规则 | 1,600 |
| 完整性 | `ok` |
| 外键违规 | 0 |
| 跨领域主体冲突标记 | 0 |

正式库经 SQLite 只读连接和在线备份生成 fresh 副本。分类上下文导出使用 `query_only=true`，导出前后副本 SHA-256 均为：

`217C933C91B96C82CC43617CF38D94BBB0950657EE6F3901AAA24DCD6121382B`

## 旧系统规则清理边界

- Rust 契约验证：过期 `catalog-rule-%` 行物理删除；用户规则与当前托管规则保留；重复应用幂等。
- 累积隔离副本验证曾分别清理 3 条和 2 条旧系统规则，完整性 `ok`、外键违规 0。
- fresh 正式库副本从 v5 协调到 v6 时，旧槽位由 v6 内容更新并新增 509 条规则；没有写回正式数据库。

## 验证层级

- 前端单元/领域合同：78/78。
- Rust/SQLite：70/70。
- Playwright：18/18。
- Sites 回退：4/4。
- TypeScript、Vite 生产构建、Rust 格式和 `git diff --check`：通过。
- Windows `--no-bundle` EXE：通过。
- BAT 真实桌面用户路径：待南烛枫验收。
- 安装包和 GitHub Release：未执行。
