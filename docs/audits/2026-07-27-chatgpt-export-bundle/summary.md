# ChatGPT 完整导出包与附件映射审计

## 结论

`ChatGPT_20260726.zip` 不能按普通 `conversations-*.json` 处理。六个会话分片只是索引；完整来源还包含 718 个 `.dat` 实体、原始文件名表、文件库 MIME 元数据和导出清单。

软件导入器现已把完整 ZIP 作为独立格式处理：

1. 原始 ZIP 先以 SHA-256 命名规则原子归档。
2. 顺序读取 `conversations-*.json`，持久化 `conversation_id` 作为来源外部 ID。
3. 建立 `conversation_id → message_id → attachment_id → .dat entry` 映射。
4. 文件签名优先，结合 `library_files.json`、`conversation_asset_file_names.json` 恢复扩展名和 MIME。
5. 所有 `.dat` 原始字节流式解压到受控附件目录；生成包含原条目名、原文件名、哈希、格式和消息关联的 JSON 清单。
6. 被消息引用的实体同时登记为对应记录的附件；未直接关联实体仍保留在导出包附件目录和清单中。

该能力由 `src-tauri/src/chatgpt_export.rs` 单点负责。页面和普通 JSON 解析器不得再复制 ZIP、附件 ID 或格式判断规则。

## 真实导出包证据

- ZIP 大小：944,961,948 字节
- SHA-256：`A223D5D9AD25C20AAA871A1D7ABC5D6A6B326B3C1E3827D1261911E19B5630EF`
- ZIP 文件条目：733
- 解压总大小：1,060,455,031 字节
- 会话分片 / 会话：6 / 517
- `.dat` 实体：718
- 消息附件引用出现次数：451
- 去重后的消息附件 ID：450
- 能找到实体 / 缺失实体：450 / 0
- 直接关联消息 / 未直接关联消息的实体：450 / 268
- 原文件名表 / 文件库元数据覆盖：540 / 672
- 格式识别未知项：0

| 格式 | 数量 |
|---|---:|
| PNG | 316 |
| JPEG | 264 |
| Markdown | 82 |
| MP4 | 16 |
| WAV | 14 |
| PDF | 11 |
| DOCX | 5 |
| TXT | 4 |
| Python | 2 |
| CSV / HTML / JSON / MP3 | 各 1 |

完整私有清单位于 Git 忽略目录：

- `.runtime-qa/knowledge-classification-preview-20260727-chatgpt-complete-v1/chatgpt-export-inspection-v2.json`
- 报告 SHA-256：`0F2EA3CC1F75697B6183C143358BE88124130432EF30FC524D4C1761552AFE03`

仓库摘要不记录对话标题、消息正文、附件原文件名或附件 ID。

## 完整对话集合

现有旧库分类输入包含 100 条 ChatGPT 会话；新增六个分片包含 517 条，二者无 `conversation_id` 重叠，六个分片内部和相互之间也无重复。

组合分类输入为 901 条：

- 旧库基础记录：384
- 新增 ChatGPT 会话：517
- ChatGPT / Claude / 其他：617 / 266 / 18

当前临时 Topic 目录仍严重不足：901 条中只有 10 条达到候选阈值，891 条为人工处理。独立重跑的 JSON 和 Markdown 报告逐字节一致。

## 已验证与边界

已验证：

- 真实 944.9 MB ZIP 的只读结构、SHA-256、分片、消息引用、实体存在性和格式恢复。
- 合成 ZIP 的完整导入：ZIP 归档、会话持久化、来源外部 ID、扩展名恢复、附件字节一致、消息映射清单和记录附件登记。
- ZIP 路径穿越拒绝、条目数/单条目/总解压体积上限、2 GB 导入包上限。
- 大文件归档使用临时文件、同步落盘和原子重命名；不把完整 ZIP 或附件跨 IPC 发送到 WebView。
- 附件解包或清单写入失败时清理本次半成品目录，允许用户直接重试，不遗留不可恢复的占位目录。

真实导入已执行：

- 数据根：`D:\南枫知识库`
- 导入任务：`de093630-8e29-49b9-99c0-65d759b7d2cd`
- 活动记录：384 → 901；导入 517，跳过 0
- 恢复附件实体：718；逐个核对大小与 SHA-256，合计 957,817,814 字节
- 消息附件关联：451；数据库登记附件行 446。差异来自 5 个同一记录内相同内容哈希的重复实体，全部物理实体和消息关联仍保留在清单中。
- 导入后数据库完整性：`ok`
- 私有回执：`D:\南枫知识库\logs\chatgpt-import-de093630-8e29-49b9-99c0-65d759b7d2cd.json`

尚未执行安装包生成和 GitHub 发布；本轮交付边界是新目录上的完整开发与数据验证。
