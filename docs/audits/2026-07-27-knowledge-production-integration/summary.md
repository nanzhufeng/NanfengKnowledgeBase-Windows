# 正式知识生产链路接入验证摘要

## 结论

正式应用入口、Rust/SQLite 知识仓库和整理工作台已经接通；901 条真实记录的数据库副本已完成 migration v3 隔离验证。正式 `D:\南枫知识库` 尚未执行 v3，桌面窗口也未启动。

## 已验证

- 正式入口：收录箱、主题浏览器、整理工作台，不再路由到假数据原型。
- 知识对象：Domain、Topic、Source Item、判断快照、证据、待验证问题、主题关系与操作日志。
- 分类：本地确定性建议、人工确认、操作日志和撤销。
- 整理：合并影响预览、事务提交、逆操作撤销；拆分只读预览；确定性关系建议人工确认后写入。
- 安全：远程 Markdown 图片不自动联网；附件打开/删除限制在受控目录；完整迁移备份逐文件校验 SHA-256。
- 稳定性：数据库备份/恢复工作移出 WebView 线程；恢复前校验文件集合、大小、哈希和可用空间。

## 隔离迁移证据

- 隔离根：`.runtime-qa/knowledge-v3-20260727-qa1/`
- 输入：正式数据目录的 `app.db`、`app.db-wal`、`app.db-shm` 只读副本。
- migration：`1, 2, 3`
- 活动 Record：901
- Source Item：901，其中 legacy 映射 901
- 收录箱：901
- 附件登记：446
- `PRAGMA integrity_check`：`ok`
- 外键违规：0
- 第二次执行：上述数量不变，没有重复来源或第二份迁移前备份。

隔离检查器必须检测到 `.isolated-knowledge-migration-test` 标记，并明确拒绝 `D:\南枫知识库` 与 `D:\南枫情报台`，避免误操作正式数据。

## 回归

- 前端单元测试：46/46
- Rust：53/53
- Playwright 无界面交互：16/16
- Sites：4/4
- TypeScript：通过
- Vite 生产构建：通过

## 未验证与后续

- 正式 `D:\南枫知识库` migration v3。
- 真实 Tauri 桌面窗口中的 CSP、命令桥和重启持久化。
- 完整迁移备份恢复演练。
- 生产分类规则管理、FTS5/BM25 分类信号。
- 主题合并后的别名重定向。
- 安装包和 GitHub 发布。
