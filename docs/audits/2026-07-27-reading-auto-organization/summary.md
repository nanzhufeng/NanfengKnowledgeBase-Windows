# 阅读布局与自动整理验收摘要

日期：2026-07-27
代码 checkpoint：`9965ad5`

## 已实现

- 外部链接与受控附件统一由 Rust `external_open` 打开；Windows 直接使用 `ShellExecuteExW + SW_SHOWNORMAL`，并在调用前允许默认应用取得前台。
- 收录箱宽屏布局只滚动左侧来源列表；页头与右侧正文/主题归属卡保持固定。
- 正文预览占用主题归属区之外的剩余高度；长候选列表只滚动自身。
- 当前判断、TXT、Markdown、Claude/ChatGPT 等平台会话统一使用 Markdown 与角色消息卡。
- Domain/Topic 支持创建、重命名和编辑描述，保留已有 ID 与关联。
- 新导入来源自动生成分类建议；无目录时增量启用可编辑默认目录；只有 `≥90` 自动确认并记录可撤销操作。

## 自动验证

- TypeScript：通过。
- 前端单元/领域合同：55/55 通过。
- Rust/SQLite 合同：65/65 通过。
- Playwright：18/18 通过。
- Vite/Tauri 无安装包生产构建：通过。
- 收录箱布局契约：左侧滚动后页头与右侧卡位移均为 0，正文区高度大于 300 px，统一助手消息卡可见。

## 隔离可见验收

数据根：`.runtime-qa/visible-layout-auto-20260727-qa1/data`

- JSON 预览：12 条。
- 导入：成功 12、跳过 0、失败 0。
- 自动整理：分析 12、自动归类 0、待确认 12；自动建立可编辑默认目录。
- 可见滚动：左侧列表内容移动，页头与右侧正文/主题归属卡不移动。
- 正式 `D:\南枫知识库` 和 `D:\南枫情报台` 未用于本轮写入验收。

## 待用户确认

自动桌面检查在点击测试链接后，因无法可靠确认浏览器当前 URL 而按安全规则停止。代码、Rust 合同和生产构建已通过，但图片查看器/浏览器是否在南烛枫的实际默认应用环境中直接出现在前台，仍需用最新 BAT 最终确认。

## 交付

- BAT：`启动南枫知识库-测试版.bat`
- 测试 EXE：`.runtime-qa/knowledge-base-build/release/nanfeng-knowledge-base.exe`
- 测试 EXE：15,263,744 字节；SHA-256 `0B5772D1479AD8185BC4AD4DE7954B229D12805EC751A4C08B2E431C36FED6DE`
- 未生成或安装新安装包；未推送 GitHub；未创建或更新 Release。
