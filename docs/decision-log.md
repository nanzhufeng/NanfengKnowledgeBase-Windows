# 长期决策

| 日期 | 决策 | 原因 | 当前影响 |
|---|---|---|---|
| 2026-08-13 | AI结果、断点和增量复用必须同时匹配模型与执行契约；千问直连是已授权正式通道 | 仅冻结供应商/模型不能阻止Prompt、Schema或推理策略升级后误续跑、误复用；旧“只保留OpenRouter与DeepSeek”已被当前千问正式验收和代码事实替代 | `nfkb-ai-execution-v1`进入任务、断点、洞察、档案和分类修订；legacy只读不复用。正式阶段路由固定Flash处理高频批次、Plus处理跨文档结构与整合，不跨通道自动替换 |
| 2026-08-11 | Shell 定位只在最后一跳转换 Windows 长路径，并以父目录与选择项调用系统 API | 正式受控路径的 `canonicalize` 会产生 `\\?\` 前缀，适合 I/O 但不能直接传给 `ILCreateFromPathW`；同时该 API 的首参必须为文件夹而不是文件 | `external_open::reveal_path`继续先按原始规范路径完成安全校验，再将 `\\?\`/`\\?\UNC\` 转为 Shell 显示路径；以父目录 PIDL 和一个目标 PIDL 请求 Explorer 精确高亮，所有文件类型和导出入口共用 |
| 2026-08-11 | 所有“定位文件”统一使用 Windows Shell 原生选中能力，不再调用 `explorer.exe /select` | 命令行参数会受引号、中文、空格、井号及 Explorer 进程复用影响，两次局部转义修复都无法形成稳定合同；Windows 官方已有专用 `SHOpenFolderAndSelectItems` API | `external_open::reveal_path`唯一初始化 COM、把规范化路径转换为 PIDL 并选中文件；附件只传ID、导出只传受控目录内路径，视频/图片/音频/文档/普通文件和单篇导出全部复用；真实MP4原生调用已返回成功 |
| 2026-08-10 | AI设置保存状态常驻，并把保存与模型更新拆成两个真实结果 | v94已成功写入Windows凭据库，但切换通道清空输入框、模型更新异常被统一显示为“设置保存失败”，用户无法判断Key是否丢失 | 设置页主卡统一900px列宽；已保存Key持续显示掩码，眼睛按钮按用户点击读取并显示/隐藏；保存成功立即保留，更新失败透传供应商真实错误 |
| 2026-08-10 | AI 自动整理成为当前最高优先级；只保留 OpenRouter 与 DeepSeek 直连 | 本地规则提炼对复杂主题的总结、证据边界和主题管理建议不足；OpenRouter 已覆盖 OpenAI 与 Claude，无需重复直连和复杂配置 | 新增 migration v6、Windows 凭据库、动态最新模型目录、单主题结构化洞察和逐任务 Token/费用账单；模型结果不覆盖人工对象，主题结构变更仍需一次确认；旧“UI 不出现模型依赖”阶段决策由本条替代 |
| 2026-08-09 | 当前验收入口唯一化，附件目录即时完整但实际媒体按可视范围有界加载 | 历史BAT与隔离包泛滥会启动旧EXE；同笔记同时eager加载大图、视频、PDF和文本会制造解码与内存峰值 | 根目录只保留`启动南枫知识库-当前验收.bat`；设置页显示版本、构建标签和EXE SHA-256；构建有独占锁。目录与月份分组完整呈现，图片懒加载、音视频只预载metadata、PDF/文本接近可视区才挂载 |
| 2026-08-09 | 视觉媒体明确复位并拆开视频控制、平移和窗口调节所有权 | 中键不可发现，固定64px控制区判断不随WebView2/DPI变化，两套窗口resize会造成状态分叉 | 标题栏提供`适合屏幕`；视频原生元素不承担平移，缩放后由独立手柄拖动；窗口只保留CSS原生resize |
| 2026-08-09 | 图片与视频统一为同一个视觉媒体视口，视频使用纯黑承托并允许完整等比放大 | 旧最高层预览只给图片暗色外壳和fit/custom鼠标状态，视频落回浅色棋盘且只设max-width/max-height，导致小视频无法铺满、两类预览行为分叉 | `mediaPreviewViewport`成为唯一算法所有者；图片保留原图像素上限，视频按真实宽高允许contain放大；两者共用暗色外壳、居中、调窗、Ctrl+滚轮和中键复位，视频原生控制条拥有指针优先权 |
| 2026-08-03 | `docs/design-system.md`成为其他项目引用南枫知识库开发设计的唯一入口 | 详细规则分散在设计基线、验收矩阵、架构、视觉经验和协作复盘中，原设计系统摘要过短；只说“参考南枫知识库”容易漏掉滚动、焦点、返回、命中范围、弹窗、预览和证据分层 | 软件开发根级与项目`AGENTS.md`负责自然语言路由；`design-system.md`只总结和链接，精确事实仍由原权威文档持有；默认不外带知识库业务、固定尺寸、背景图、数据结构和历史证据 |
| 2026-08-03 | 现行合同与历史证据分层，用户可见入口只使用`主题洞察 / 全部笔记 / 主题管理` | 主规格、交接、设计基线和验收矩阵仍混有旧阶段、旧名称和旧 BAT，可能让后续开发恢复已否决方向 | 产品目标、当前入口、架构所有者、设计和验收各有唯一文档；旧名称、版本号、截图、BAT与隔离包只作兼容或历史定位，不取得现行规则所有权 |
| 2026-08-03 | 中性玻璃边缘使用低对比渐退，完整边界只服务明确状态 | 项目规则中的“闭合连续描边”会把旧白线问题重新写回所有卡片，并与现行材质基线冲突 | 禁止连续等亮白线、闭环白描边和统一渐变描边；选中、焦点、警告等语义状态仍可使用完整状态边界 |
| 2026-08-02 | 图片预览以原始像素建立渲染面，适合窗口比例与实际像素缩放分离 | 旧实现先把图片强制适配为画布大小，再放大适配结果；百分比不是原图像素比例，放大检查细节时可能复用低分辨率栅格 | 唯一`AttachmentPreview`读取`naturalWidth / naturalHeight`，100%代表原图1:1；中键仍恢复当前窗口完整适配，已放大/平移状态不因调窗跳回 |
| 2026-08-02 | 删除来源档案正文顶部重复大标题 | 侧栏已经明确入口身份，大标题不帮助判断、定位或操作，只挤占有效内容空间 | 来源页顶栏改为仅承载右上自动整理动作，列表整体上移；侧栏名称、搜索、筛选、列表和详情语义不变 |
| 2026-08-02 | 图片附件预览环境统一暗底，中键作为“恢复当前窗口完整适配”的固定手势（已被2026-08-09视觉媒体统一规则扩展） | 浅色外围削弱图片主体；缩放或拖动后需要快速回正方式 | 历史阶段只覆盖图片；当前现行合同以本表2026-08-09图片与视频统一视口、明确按钮和分离命中所有权为准 |
| 2026-07-25 | 核心离线可用，不强制模型 API | 降低成本、维护和稳定性风险 | UI 不出现模型依赖 |
| 2026-07-25 | UI 与交互先于数据库 | 先确定真实工作流，减少后续返工 | 视觉契约已完成；生产路径已切换为 SQLite，浏览器数据只用于 UI QA |
| 2026-07-25 | JSON 是第一优先导入格式 | 适配 ChatGPT 与结构化资料归档 | 桌面端已实现归档、预览、字段映射、去重策略、日志和错误隔离 |
| 2026-07-25 | 使用单一深色左栏和浅色三栏工作台 | 保留科技质感，同时维持长内容阅读性 | 主界面结构固定 |
| 2026-07-25 | 卡片统一使用向下投影和克制的物理动效 | 提升卡片交互质感，不让界面漂浮失序 | 全部卡片复用共享 Token |
| 2026-07-25 | 红、橙、绿承担语义色，蓝色只作主操作与结构色 | 避免大面积单一蓝色 | 判断/问题/事实分色 |
| 2026-07-25 | 关联只使用细线和锚点 | 保留结构关系但不抢内容 | 选中记录到详情的低调连接 |
| 2026-07-25 | 阶段 1 视觉与交互契约冻结，进入完整本地 MVP | 用户已确认方向并明确要求实施到完成 | 不再以“原型不可写入”为边界 |
| 2026-07-25 | Tauri 2 + Rust rusqlite 是生产数据唯一所有者 | 保持离线、可审计、可迁移并减少双写 | 前端只经 `RecordRepository` 调命令，不直接执行 SQL |
| 2026-07-25 | 浏览器仓库仅作视觉和契约适配器 | 保留快速浏览器 QA，同时避免把演示数据当桌面真相 | 桌面运行必须以 SQLite 结果验收 |
| 2026-07-25 | 删除采用回收站 + 完整标题确认；版本恢复生成新版本 | 保护本地研究资料，避免覆盖历史 | 标题重输部分已被 2026-07-26 的简单确认决策取代；活动记录保护和版本恢复规则仍有效 |
| 2026-07-25 | 导入先归档原文件再解析，并用 SHA-256 识别重复 | 原始资料必须可追溯，解析失败也不能丢失输入 | 导入预览和写入都引用受控归档文件 |
| 2026-07-25 | 数据库恢复前强制生成安全备份并执行完整性检查 | 恢复属于高风险数据替换 | UI 二次确认，Rust 服务负责校验与回滚证据 |
| 2026-07-25 | Windows 正式数据默认放在 `D:\南枫情报台`，旧 AppData 仅做无损复制迁移并保留 | 用户明确指定 D 盘，同时必须防止升级迁移造成资料丢失 | 历史决策；默认目录已由 2026-07-26 品牌统一决策迁移至 `D:\南枫知识库`，旧目录继续作为无损兼容来源 |
| 2026-07-25 | 收藏命令只返回轻量状态，不返回整篇记录正文 | 大正文跨进程回传与重复点击会放大 WebView/IPC 稳定性风险 | UI 本地合并收藏状态，并按记录号阻止并发重复提交 |
| 2026-07-25 | 导入预览只跨 IPC 返回有上限样本，确认时由 Rust 重读归档原件 | 200 MB 文件若把全部正文往返 WebView 会造成卡顿或崩溃 | 最多展示 32 条映射样本；完整记录数、边界计数和最终写入均以后端归档原件为准 |
| 2026-07-25 | 导入日志只保存字段摘要、正文长度和 SHA-256 | 原文件、记录正文和日志三份重复正文会放大数据库体积 | `import_job_items.raw_json` 不再复制完整 `sourceText` |
| 2026-07-26 | 永久删除只保留一次明确确认，不再要求重输标题 | 记录已先进入回收站，重复输入增加操作负担但没有显著提高保护 | 活动记录仍不能永久删除；回收站确认后才执行 |
| 2026-07-26 | 原始内容日期独立存入 `records.original_at` | 导入时间不等于对话或资料发生时间 | 列表、筛选、排序、详情和导出优先使用原始日期 |
| 2026-07-26 | 附件文件操作在后台线程使用堆缓冲并原子提交 | 线程栈上的 1 MB 数组触发过 `0xc00000fd`，且同步复制会阻塞 UI | 临时文件失败即清理，数据库失败不残留新归档 |
| 2026-07-26 | Codex/Obsidian 通过 Vault/Markdown 交换层协作，不能直接写 SQLite | 保留开放整理能力，同时维持一个正式数据所有者和可追溯版本 | 导出可审阅文件，回流必须经过导入和版本机制 |
| 2026-07-26 | Markdown 展示采用 GFM + 安全清洗 + Obsidian 常用语法子集 | 需要笔记软件式阅读效果，也必须避免导入 HTML 执行风险 | 源文保持不变，展示层支持表格、任务列表、callout、wikilink 和附件 |
| 2026-07-26 | 记录选择联动使用纯亮橙色且不使用渐变；侧栏选择恢复红色 | 渐变破坏卡片质感；侧栏与内容联动需要形成明确层级，用户要求保留原有红色导航识别 | 记录边缘、关联线、锚点和详情边缘使用 `#ff681f`；侧栏选中和安全/破坏语义继续使用红色 |
| 2026-07-26 | 快速定位滑块只负责列表滚动，不隐式切换当前记录 | 拖动时连续选择会触发详情读取、连线重算和多次滚动，造成列表乱跳 | 滑块拥有独立位置状态；只有用户点击记录时才更新详情选择 |
| 2026-07-26 | 详情与完整内容共用一个 Markdown 阅读器，详情显示约一页，完整弹窗默认占窗口 85% | 两套样式会割裂，旧尺寸持久化会覆盖新默认值，过短预览和空白都降低信息密度 | 详情预览最多 2400 字符并保留 Markdown；完整内容弹窗不恢复旧尺寸但仍可手动缩放 |
| 2026-07-26 | 外部链接只交给系统默认应用，不在主 WebView 内导航 | 外站替换应用会失去明确退出路径，也会暴露第三方页面的传统滚动条和布局 | Tauri 使用 opener 权限，浏览器 QA 使用独立窗口回退；应用主工作区保持不变 |
| 2026-07-26 | 会话图片采用“引用保留 + 受控附件同名关联”，不从 UUID 推断不存在的二进制 | 现有 conversations.json 保存了引用和文件名，但正式数据目录没有对应图片文件 | 未关联时显示占位和添加入口；补充同名附件后通过受限 asset protocol 懒加载显示 |
| 2026-07-26 | 单篇记录取消平台分享按钮，提供完整 Markdown 与 DOCX 两种文件导出 | 微信、QQ、Telegram、ChatGPT 不能由当前本地应用可靠完成跨平台直达分享；复制动作也不是用户要求的真实导出 | 两种输出统一由完整记录原文组成并写入受控 exports 目录；成功提示可打开原路径 |
| 2026-07-26 | 换机备份采用可校验文件夹协议，不把日志和历史导出伪装成必要数据 | 迁移必须覆盖正式数据库、历史版本、回收站、附件、导入原件与界面偏好，同时避免备份递归和无意义体积膨胀 | `manifest.json` 声明协议版本；恢复前生成当前状态完整安全备份，失败自动尝试回滚 |
| 2026-07-26 | Windows 主窗口默认尺寸设为 1702×1066 | 与用户确认的图一信息密度和卡片比例一致 | 新启动默认使用该尺寸；不生成安装包前通过 BAT 测试版验收 |
| 2026-08-01 | 主预览和最终视觉验收固定为 1702×1066、100% 缩放、1× 密度 | 不同大小的交付窗口会改变卡片比例、换行和首屏密度，导致南烛枫无法直接比较修改前后 | 前后截图必须同主题、同数据、同状态、同裁切；右侧/底部分栏和其他临时视口不得作为交付窗口或正式对比证据，响应式专项必须单独标注 |
| 2026-08-01 | 知识视图与主题管理的领域→主题中栏由同一共享组件持有 | 共享 CSS 不能阻止两页 JSX、数量含义、空态和折叠逻辑继续漂移 | `KnowledgeTopicHierarchy`成为唯一实现源；页面只传搜索/筛选结果、当前主题与选择回调，禁止复制层级列表结构或私有折叠状态 |
| 2026-08-01 | 右侧主体阅读字号从旧1.3倍版本统一缩小到0.9 | 普通长正文、知识解释卡和会话Markdown在真实桌面仍略显偏大 | 共享Token最终为原始字号1.17倍，Markdown标题同步缩小；链接、按钮、元数据、左侧列表和页面布局不变 |
| 2026-08-01 | 四个单篇列表入口的共同问题默认一次修全 | 四入口已共享底层框架，逐页等待重复反馈会重新制造行为和视觉分叉 | 日期统一`YYYY-MM-DD`，默认紧凑，图标/标题/元数据空间与显示工具栏由`UnifiedNoteListCard`体系唯一持有；卡片模式/排序操作组以`right:0`固定，右边缘必须与工具栏重合，120/999三位数状态是最小压力合同；南烛枫只指出一个入口时，先判断是否为共享问题，是则同步覆盖四入口 |
| 2026-08-01 | 四入口筛选与显示工具栏必须真正单一所有者，右贴不得改动左摘要 | 仅共享卡片而保留来源页私有筛选仍会造成结构和关闭行为分叉；通过压缩摘要给右侧让位又会吞掉入口名 | `UnifiedNoteListFilter`唯一持有筛选结构、字段渲染和关闭行为；入口只传字段/状态。显示工具栏把左摘要和右操作独立绝对定位，左侧入口名、计数、单位完整保留，右侧继续`right:0`，两者都不可互相侵占 |
| 2026-07-26 | ChatGPT `mapping/current_node` 会话由软件本地确定性整理 | 新导入记录不能依赖 Codex 手工修正文；原始树包含内部思考和废弃分支，直接展示会暴露无价值 JSON | 展示沿当前节点父链还原角色、日期和资源，只显示可见文本；原 JSON 保真保存，既有记录升级后自动生效 |
| 2026-07-26 | 详情卡取消重复的橙色左竖线 | 列表边框、关联线、详情边框和内容卡同时强调造成视觉噪音 | 保留列表选中与关联结构，内容区使用普通中性卡片边界 |
| 2026-07-26 | 批量导入按文件顺序执行，单项失败不终止整批 | 同时解析多个大文件会放大内存、磁盘和 WebView 压力；用户仍需要一次拖入和统一处理 | 选择器与桌面拖拽接收多路径，队列最多 100 项；每项继续走原有归档和后端重读协议，可单独映射或全部自动识别导入 |
| 2026-07-26 | Markdown callout 必须保留源类型语义，不允许统一套用单一强调色 | Obsidian 的成功、警告、危险、信息和引用表达不同含义；同色会削弱阅读层级并误导状态 | 统一阅读器负责类型别名、图标和受控语义色；普通引用保持中性，源 Markdown 不改写 |
| 2026-07-26 | 产品统一更名为“南枫知识库”，正式图标采用深蓝 N 与蓝色枫叶 | 名称更准确覆盖知识归档、检索、版本与开放文件工作流；图标由用户指定参考方向 | 用户可见名称、导出文件、测试入口和默认数据目录统一更名；旧数据目录、备份协议、偏好键、应用标识和环境变量仅作无损升级兼容 |
| 2026-07-26 | `docs/南枫知识库_产品定义与自动分类主规格.md` 成为当前最高产品规格 | 用户重新定义产品核心为分类、主题结构、跨时间整合、判断演化与上下文复用 | 旧“记录管理”定位降为兼容基础设施；产品边界、架构和测试计划按主规格重写 |
| 2026-07-26 | 原型确认前不新增主题表、不迁移正式数据 | 新规格改变核心对象和入口，直接改库会把未确认的信息结构固化并危及现有资料 | `/?prototype=knowledge-evolution` 使用完全隔离的假数据；生产 `App` 与 `RecordRepository` 不受影响 |
| 2026-07-26 | Source Item、Note、Topic、Judgment Snapshot、Evidence 和 Open Question 必须是独立领域概念 | 现有 `Record` 已承担过多职责，继续堆字段会导致分类、合并、证据和时间线规则互相污染 | 原型确认后先完成对象职责、唯一所有者和可回滚迁移设计 |
| 2026-07-26 | 分类固定在内容提取与标准化之后，并优先使用本地确定性五层评分 | 未提取原始 JSON 或不可见内部字段会造成错误归类；离线、可解释和可复现优先于模型效果 | 分类器需显示分数、原因、候选、规则版本和操作日志；模型只可作为未来可选辅助 |
| 2026-07-26 | 主题合并必须 preview/快照/别名/重定向/undo；拆分首版只预览 | 合并和拆分会重写长期结构，错误代价高且需要完整可追溯 | 整理工作台先实现影响预览；拆分不提供直接提交 |
| 2026-07-26 | 确定性分类器只返回建议，不能直接写数据库 | 自动接受资格、用户确认和实际持久化是不同职责，混在分类器中会破坏撤销与入口一致性 | `classifySource` 是唯一评分入口；未来 Classification Use Case 统一负责接受、修改、日志与撤销 |
| 2026-07-26 | FTS5/BM25 由持久化适配器提供归一化分数，分类器只组合信号 | 领域层不能依赖 SQLite 查询实现，但全文相关仍必须进入统一五层评分 | 原型无适配器时使用确定性词项相似度；生产必须注入可复现的本地检索分数 |
| 2026-07-26 | 未来知识表先以隔离 schema contract 验证，不直接加入启动 migration | 表约束、FTS 同步和对象职责需要先在内存库证明，直接接入正式库会把未完成的兼容策略固化 | `src-tauri/src/knowledge/schema.rs` 可编译、可测试但没有运行时调用；正式 migration 需单独授权 |
| 2026-07-26 | legacy Record 迁移预演只把标签视为主题候选，不把标题自动升格为主题 | 标签可能多义，标题通常是一条来源或笔记的名称；自动升格会生成大量碎片主题 | 单标签仍需确认，多标签标记歧义，无标签进入未分类；回收站默认排除且预演严格只读 |
| 2026-07-27 | 正式旧库 384 条记录无标签，迁移前必须增加内容驱动分类 dry-run | 正式只读报告确认 legacy 标签无法提供任何 Topic 候选；直接迁移会把全部内容留在未分类或诱发标题升格 | 分类继续复用本地确定性评分并只输出建议；标题永不自动成为 Topic，正式写入仍需单独授权 |
| 2026-07-27 | 正式库审计采用只读在线备份，完整报告只留在 Git 忽略目录 | WAL 数据库需要一致性快照，报告又包含真实标题等私有内容 | 正式连接固定 `READ_ONLY` + `query_only`；主库/WAL 元数据前后核对，仓库只提交脱敏摘要 |
| 2026-07-27 | `prototype-seed-v1` 只作为目录覆盖测试，不得成为正式 Topic 树 | 真实 384 条记录中只有 7 条达到候选阈值，覆盖率约 1.8%；原型 fixtures 明显不代表用户完整资料结构 | 下一阶段先生成并确认个人主题目录候选；不降低阈值、不堆关键词、不自动写库 |
| 2026-07-27 | legacy 分类 dry-run 必须复用 `classifySource` | Rust、迁移脚本和前端若各有评分规则，会导致阈值、解释和最终迁移结果分叉 | Rust 只读导出标准输入，TypeScript 编排并调用唯一分类器；报告不是持久化结果 |
| 2026-07-27 | ChatGPT 完整导出 ZIP 是原始来源，`conversations-*.json` 只是会话索引 | 消息只保存附件 ID，真实图片、文档、音视频位于 `.dat`；仅导入 JSON 会永久丢失二进制与来源关系 | `chatgpt_export.rs` 统一维护消息—附件—实体映射、格式恢复、流式落盘和清单；原 ZIP 保真归档 |
| 2026-07-27 | `.dat` 格式恢复采用“签名优先、元数据补充、未知不猜测” | 文件名表和文件库元数据覆盖范围不同，扩展名也可能与真实内容不一致 | 强签名决定 PNG/JPEG/PDF/ZIP/WAV/WEBP/MP4/MP3；DOCX 由 ZIP 签名与官方 MIME 联合确认；无法确认时保存为 `.bin` 并报警 |
| 2026-07-27 | 分类 v2 只消费用户可见正文，并要求直接主题证据后才展示候选 | 原始会话 JSON 中的模型名、搜索元数据和附件字段曾把 `Claude`、`Google`、`SRT`、`ACES`、`GPU` 等误判为内容主题；0 分候选进一步放大误导 | Rust 分类适配器投影当前可见消息；个人目录停用旧版宽泛托管规则并补齐常见内容主体；低于 45 分或只靠历史/相对全文信号时返回空建议 |
| 2026-07-27 | 分类 v3 在保持直接证据门槛的同时扩大目录覆盖，并把标题与可见正文统一为内容证据 | v2 消除了乱猜，但正式库仅有 21 个主题，且关键词规则只查正文；一个明确短语加单项 BM25 通常约 40 分，造成楼市、Git、备份恢复、视觉设计等清晰内容大量无候选 | 个人目录扩展到 47 个主要资料主题；托管关键词强度校准为 0.9，标题与正文共同匹配，BM25 必须在可见投影中命中，长正文集合相似度采用确定性采样；45/70/90 阈值和仅 ≥90 自动确认不变 |
| 2026-07-28 | 分类 v6 采用“标题主体边界 + 正文有限补充”的通用架构，并物理协调过期系统规则 | 仅补截图样本会继续遗漏；长正文偶然词、通用后缀和旧托管规则会制造跨主题误判，且不能把清理责任留给用户 | 清晰标题过滤跨主体候选，无主题标题只读取开头 600 字；宽泛词、实体、历史活跃度和 FTS 只能佐证；目录协调物理删除不再存在的 `catalog-rule-%`，用户自建规则不受影响；fresh 901 条矩阵确认清晰正文无候选和主体冲突均为 0 |
| 2026-07-28 | 日常知识工作区收敛为“来源档案、主题结构、知识视图”三个入口 | 收录箱与来源档案、主题浏览器与整理工作台职责重叠，人工分类流程遮蔽了知识成果 | 收录箱并入来源档案并仅把低置信/冲突项作为例外；整理工作台并入主题结构且高级维护默认折叠；知识视图以领域→主题→命题为阅读主轴 |
| 2026-07-28 | 命题允许按假设组并列竞争，并记录置信度、推翻条件和事实有效期 | 单一覆盖式结论无法表达不确定性，长期知识也不能默认永久有效 | migration v4 追加命题类型、假设组、确认/截止/复核日期；证据和判断可直接关联命题，历史命题仍用 supersede 保留 |
| 2026-07-28 | 决策账本是独立对象，不用普通笔记替代 | 决策需要同时保留当时依据、已知风险、实际行动、结果与复盘，覆盖式编辑会丢失决策上下文 | Decision 可关联命题和判断快照；结果状态、复核时间、最终结果和复盘独立保存并进入主题研究上下文 |
| 2026-07-29 | 四张最终页面图锁定核心组织/功能布局，五套皮肤锁定独立视觉层 | 过去把“预览确认”笼统处理，导致数据模型、页面结构和皮肤工作互相替代，已确认成果只留在会话临时图中 | 将四张图纳入 `docs/screenshots/final-core-workspace/`，由 `core-workspace-design-baseline.md` 明确锁定类别；后续只修改南烛枫明确要求变化的层级，其他锁定层级必须继承 |
| 2026-07-29 | 自动分类改为最高候选严格超过 65 分即默认归类 | 南烛枫明确认为原 90 分门槛过高，日常使用应自动化优先，人工只处理例外 | `>65` 采用稳定排序最高候选并写可撤销操作；`45–65` 保留候选；`<45` 人工处理；新导入与历史待整理统一使用该合同。此条替代 2026-07-27 记录中的旧阈值结论，不改写历史记录 |
| 2026-07-29 | 来源列表定位、正文搜索和三点菜单采用全局明确交互合同 | 把正文搜索误叫“定位”导致真正的长列表定位被删除；操作平铺又挤压正文，三点菜单不自动收起会残留遮挡 | 来源列表用独立滑块只滚动不选中；正文按钮统一称“搜索”；收藏、跟踪、导出、完整正文和软删除进入三点菜单，点击空白或按 Escape 关闭 |
| 2026-07-29 | Legacy Record 的回收站状态决定对应 Source Item 是否出现在来源档案 | 旧实现只更新 `records.is_deleted`，来源查询仍按 `source_items.status` 返回，造成“回收站计数增加但笔记仍在” | Repository 查询关联 legacy record 并排除已删除记录；恢复后重新显示；Rust 契约同时覆盖删除和恢复 |
| 2026-07-30 | 历史入口曾统一为“知识视图、来源档案、主题管理”（已由2026-08-03新名称替代），可见时间线只归判断演变 | “主题结构”作为入口名会与底层结构概念混淆；独立时间切片控件和多状态时间线造成职责重复 | 时间线职责继续有效；旧入口名称仅作历史证据，当前用户可见名称为`主题洞察 / 全部笔记 / 主题管理` |
| 2026-08-01 | 四入口单篇卡右侧三个快捷操作统一采用“悬停显示” | 卡片本身可聚焦，旧`:focus-within`会把点击选中误判为操作焦点，导致按钮在选中后一直显示 | 选中状态和卡片自身焦点不显示；鼠标悬停显示；键盘实际聚焦操作按钮或更多菜单展开时保持显示；四入口只使用共享卡片规则，不增加页面私有分支 |
| 2026-08-01 | 四入口组合筛选不仅统一外壳，还统一字段语义、选项来源和匹配 | 来源档案曾把`sourceType`作为“来源”、把`待确认/已归类`作为“状态”，另外三个入口又用“标签”替代“主题”，造成同名字段不同义 | 固定为`来源/状态/主题/起始日期/结束日期`；来源使用卡片真实来源，状态使用四种Record状态，主题使用同一正式主题目录；来源整理状态继续留在专属状态行 |
| 2026-08-01 | 主题排除规则由系统持有，日常页只展示有效结果 | 排除信号能防止相似关键词误归类，但让普通用户补写规则类型、权重和关键词会把技术配置变成日常负担；宽泛全局屏蔽又会制造漏归类 | v7为非职业主题默认托管`招聘启事/岗位职责/简历投递`0.55排除信号，职业主题不排除；无规则隐藏，有规则只读摘要并说明不会删除来源；缺少规则不进入待处理，完整编辑仅在高级维护 |
| 2026-08-01 | 笔记身份与导入原件分离，四入口来源名称由统一目录唯一持有 | ZIP/JSON分片和零散文件名被直接当作来源，造成同一提供方多套名称；旧哈希只能识别整包重复，无法表达“一篇笔记来自多个导入原件” | 同提供方稳定ID或标准化可见正文决定精确重复；重复时复用笔记并追加origin；相似标题不自动合并；单篇文件归入`零散文件导入`；目录可重命名且同步四入口，原文件证据不变 |
| 2026-08-02 | 四套皮肤共用同一外框几何，移除铜金发簪 | 原版浅色使用另一套外层留白、圆角和内部起点，切换时整体位置跳变；铜金发簪不再需要 | `styles.css`唯一持有四套共享几何；原版浅色只保留自身颜色与材质差异；`knowledgeSkins.ts`删除铜金发簪，旧保存值回退默认皮肤，不增加替代项 |
| 2026-08-01 | 单篇文件的有效标题优先于正文日期 | 历史MD以`---`占位，读取层过滤HTML标题后抓取第一个`## 日期`，导致“2026年4月5日”冒充“052 个人八字丙午年壬辰月” | 标题唯一规则按显式标题、非日期显著标题、有效文件名、正文首行依次回退；新导入与历史通用标题回填复用同一规则，非通用用户标题不覆盖 |
| 2026-08-02 | 知识四态Tab后直接进入主要内容，小字服从全局价值门槛 | Tab、模式眉题、编号步骤卡、重复计数、技术空态和重复详情曾连续复述同一职责，挤占真正的假设、时间线、正文与决策链 | 删除重复说明层和无价值小字；仅保留能帮助判断、定位或操作的日期、来源、状态、置信度、有效期、证伪条件和真实风险；四态统一14px正文起始间距，不改变数据或对象所有权 |
| 2026-08-02 | 知识摘要卡的查看操作统一打开完整内容弹窗 | 两条紧凑预览能守住首屏密度，但旧按钮只切换阅读状态，用户无法确认被省略的第三条和完整长文本 | 四张卡共用一个弹窗状态与样式；弹窗无截断展示全部摘要，支持关闭、Escape、遮罩和进入对应深读页，卡片继续只保留两条预览 |
| 2026-08-03 | 固定筛选外壳与真实滚动视口共同拥有顶部圆角裁切 | 只给`sticky`外壳设置圆角时，其透明角下仍会绘制矩形滚动内容，真实WebView2上滑会露出直角白卡与可读文字 | 滚动视口同步14px顶部圆角裁切；提高固定珍珠雾面与底部渐退遮蔽，只留不可辨认色影；不恢复曾导致WebView2纵向亮块的嵌套`backdrop-filter` |
| 2026-08-09 | 三套场景皮肤的全入口前景卡统一提亮 | 南烛枫真实WebView2截图确认v61低曝光RGB幅度使前景内容、列表和判断卡整体显灰；问题属于共享明度角色，不是透明度、模糊或大承托层职责 | 由共享明亮前景Token覆盖主题洞察、全部笔记、主题管理、收藏、跟踪、回收站和设置；保留原透明度、模糊、边缘、几何与语义色相，不提高大承托层白度，不改原版浅色、浮层/弹窗或主题整合主要来源阅读卡 |
| 2026-08-09 | 所有受控附件统一按附件ID在系统文件管理器中定位 | 视频、图片、PDF和其他文件需要从预览直接回到物理文件；若页面直接传路径或各格式各写一套，会绕过受控目录并再次产生入口分叉 | `AttachmentLocateButton`只负责共享动作；`RecordRepository.revealAttachment`只传附件ID；Rust重新读取数据库记录并通过`controlled_attachment_file`校验后调用`external_open::reveal_path`，Windows Explorer直接选中文件，定位过程不改写附件 |
| 2026-08-09 | 关联笔记与来源使用完整卡片加独立滚动 | 南烛枫真实WebView2截图确认数量越多，固定高度Grid越会压缩隐式行并裁切第二行信息；信息完整性优先于同屏卡片数量 | 每张来源卡保持完整标题与来源类型、日期、置信度；数量增加只延长右列滚动内容并可滑到末项，不改三栏比例、数据、材质或主体/外链双点击边界 |
| 2026-08-09 | 附件声明目录与受控文件实体分层，当前笔记默认自动加载 | 旧检索只查`attachments`表，导致只有手动恢复过的3个视频出现；逐项“点击恢复”又把默认内容错误变成用户维护任务 | 分类筛选始终读取声明目录与已受控实体并集并按月分组；当前笔记打开后按唯一ID顺序自动物化，单项失败隔离；图片、音视频、PDF/TXT原位呈现，不再要求逐项确认 |
| 2026-08-09 | 图片与视频时间线直接使用缩略图/代表帧 | 文件名行无法快速辨认视觉内容，也让视频播放入口不够直接 | `AttachmentTimelineMediaCard`唯一生成图片缩略图与视频内存代表帧；分类打开后顺序补齐缺失受控实体；点击复用既有最高层预览/播放器，不生成第二套播放逻辑，也不改写原媒体 |
| 2026-08-09 | Markdown附件使用受控解码与统一语义阅读器 | 原始本地文件iframe由WebView猜测Windows代码页，导致中文乱码、源码直出和整框横向滚动；单纯改样式无法修复数据解释 | Rust受控命令按UTF-8/BOM/UTF-16/GBK读取最多8MB文本；`AttachmentTextPreview`统一供正文与最高层预览消费，Markdown交给现有阅读器，原文件不改写 |
## 2026-08-01：知识视图摘要由统一读取模型自动生成并前置到首屏

- 决定：新增唯一`buildKnowledgeOverview`，优先读取正式判断、命题、证据、问题和决策；缺失时复用`buildKnowledgeSynthesis`从真实正文生成可追溯的只读摘要。
- 决定：首屏使用一条当前判断和四张紧凑摘要卡；完整事实/证据/问题/决策仍归既有四个阅读状态，不新增第五个状态。
- 决定：自动项显式标记，事实线索与证据片段分开；建议行动保持未执行语义，不写回数据库。
- 拒绝：继续让知识视图显示空卡、要求用户手工补齐所有结构化字段，或把参考图的大面积卡片原样放到长正文下面。
- 验证：前端166/166、TypeScript、Vite生产构建、1702×1066 Chrome E2E及参考/实现同屏视觉比较通过；正式Windows WebView2与正式数据内容质量未执行。
