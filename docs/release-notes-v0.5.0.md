# 南枫知识库 Windows v0.5.0

发布日期：2026-08-14

## 本次更新

- AI 全库分类与主题洞察支持协作式暂停、断点续跑、最小子集恢复和超时缩批，已完成的批次不会因后续失败丢失。
- AI 设置收敛为唯一任务路由基准：自动规划按千问直连、DeepSeek直连、OpenRouter兜底顺序选择；人工选择明确区分通道和模型，并在任务开始时冻结。
- 新增只读调用记录，按阶段显示模型、状态、Token 与失败摘要；不展示 Prompt、知识正文、API Key 或缓存键。
- 已保存 API Key 默认仅显示掩码，眼睛按钮位于输入框内右侧；手动模型目录精简为本知识库有明确用途的选项。
- 暗色场景玻璃、搜索/筛选弹层、悬浮滚轮归属和跨入口前景卡材质完成定向修复。

## 已验证

- TypeScript 通过。
- Vitest：37 个文件、220 项通过。
- Rust：150 项通过，1 项会真实打开 Windows 资源管理器的测试按设计忽略。
- Vite/Tauri NSIS x64 构建通过，`git diff --check`通过。

## 已知边界

- 安装包未进行代码签名，Windows 可能显示 SmartScreen 提示；安装前请核对 Release 中的 SHA-256。
- 本次未执行真实 WebView2、正式资料库迁移、真实 API Key 读取、在线模型目录刷新或会产生费用的 AI 调用。
- 全量浏览器视觉用例仍含一条历史暗色皮肤固定 RGB 断言，未作为本次发布通过证据。
