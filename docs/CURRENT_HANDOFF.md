# 当前交接

## 当前任务

- 目标：在冻结的阶段 1 视觉契约上完成本地优先 Windows 桌面 MVP。
- 当前实现：Tauri 2 + React/TypeScript + Rust rusqlite；真实记录、标签、来源、版本、搜索、自动保存、收藏、状态、回收站与确认永久删除已经接入。
- 当前工作区：本轮有未提交的阶段 2 增量；不得清理、重置或覆盖。

## 验证等级

- 已实现：SQLite 迁移与受控数据目录；记录 CRUD；标签/来源关系；收藏与状态；650 ms 自动保存；版本追加/预览/恢复为新版本；回收站恢复；完整标题确认永久删除；FTS5 trigram 与 1–2 字 LIKE 回退；设置中的路径、完整性检查和索引重建。
- 契约测试：前端仓库 2/2；Rust 数据库 4/4。
- 浏览器运行验证：新增 → 中文搜索 → 判断自动保存 → 追加版本 → 回收站 → 恢复 → 再次删除 → 完整标题确认永久删除，全部走通；验收数据已清理。
- 视觉运行验证：关联线两端 8 px 锚点圆心与 1 px 水平线同轴；冻结的深色侧栏、冷灰底、白卡、底部阴影和卡片动效保持不变。
- 尚未完成：真实文件导入、导入日志、导出、备份恢复、全字段编辑和标签管理界面、Windows 安装包与 Tauri 实机用户路径。
- 当前浏览器验证使用 `BrowserRecordRepository`，不是桌面 SQLite 生产证据；数据库生产证据来自 Rust 测试，桌面壳运行仍待验证。

## 下一步

1. 完成全字段记录编辑和标签管理，收口阶段 3。
2. 实现 JSON/Markdown/TXT/HTML 的原文件归档、SHA-256、预览、映射、去重与导入日志。
3. 实现单条/批量导出、数据库备份和安全恢复。
4. 运行 Tauri 桌面真实路径、Windows NSIS 构建和安装升级数据保留验证。

## 继续提示词

继续 `南枫情报台` 完整 MVP，不回退为原型。先读 `AGENTS.md`、`docs/CURRENT_HANDOFF.md`、`docs/architecture-governance.md`；保护当前未提交增量。按“全字段编辑与标签 → 文件导入 → 导出备份恢复 → Tauri/NSIS 真实验证”顺序执行，每步只做最小充分验证，并区分浏览器适配器、Rust 测试和真实桌面证据。
