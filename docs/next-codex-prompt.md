# 下一轮可直接使用的提示词

```text
继续“南枫知识库”分类 v6 的 BAT 用户验收。

仓库：C:\Users\Administrator\Documents\软件开发\nanfeng-intelligence
当前分支：codex/nanfeng-knowledge-production-checkpoint-20260727
代码 checkpoint：4b6b87e

先完整读取项目根 AGENTS.md、docs/CURRENT_HANDOFF.md 和本文件，再运行：
git status --short
git log -5 --oneline --decorate

不要扫描全部历史审计、旧原型或完整聊天记录。先简短确认：
1. `local-rules-v6` 和个人目录 v6 已完成，目录为 10 个领域、68 个可编辑主题、1,600 条有效托管规则；
2. 分类改为标题主体优先：清晰标题会过滤跨主体候选；无主题标题只使用正文开头 600 字识别主体；宽泛词、平台实体、历史活跃度和 FTS 只能佐证，不能单独制造主题；
3. 应用目录时会物理删除不再存在的 `catalog-rule-%` 旧系统规则；用户自建规则不匹配此前缀，不在清理范围；
4. 首次加载会自动处理整个收录箱，并按当前版本完成标记断点续算，不再要求用户逐条点击；
5. fresh 正式库只读副本审计覆盖 901/901：886 条有候选，15 条为空/极短/无业务主体，清晰正文无候选为 0，跨领域主体冲突标记为 0；
6. 全量升级只保存建议和无候选完成标记，不改写正文、不覆盖人工确认、不自动确认 90 分以下候选；
7. 正式库没有写入 v6 建议，也尚未执行旧系统规则协调；这些只会在南烛枫运行 v6 BAT 后发生；
8. BAT 已指向 `.runtime-qa\knowledge-base-build-v6\release\nanfeng-knowledge-base.exe`，SHA-256 为 `F1D4C52C3AFA674EF10A3F46D0A29CFF2970361FDFBDE6C767E8CFF6FE53774C`；
9. GitHub 发布未执行，Inno Setup 7 保持暂缓。

本轮唯一任务：
等待南烛枫关闭当前旧测试窗口后，由南烛枫运行仓库根 `启动南枫知识库-测试版.bat`。等待目录协调和后台全收录箱整理完成，确认旧系统规则已按 v6 目录协调、无需逐条点击即可看到新版主题建议；只抽查整体代表性内容，不再让用户承担寻找遗漏。同步确认图片/外部链接前台显示、空内容过滤、详情弹窗和左侧独立滚动。发现问题时先读取当前版本完成统计和具体失败项，再定向修复。

边界：
- 不手工删除、清空或覆盖 `D:\南枫知识库`；仅允许 v6 应用按上述合同协调系统目录、系统规则和分类建议。
- 不删除或覆盖 D:\南枫情报台。
- 不切回 main。
- 不安装 Inno Setup 7。
- 不制作安装包，不发布 GitHub；只有南烛枫明确说“上传”才进入发布流程。
- 保护工作区，不 reset/clean/stash/覆盖未知改动。
```
