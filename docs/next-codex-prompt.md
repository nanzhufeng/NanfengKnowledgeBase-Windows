# 下一轮 Codex 可直接使用的提示词

继续验收“南枫知识库”核心产品闭环。

仓库：`C:\Users\Administrator\Documents\软件开发\nanfeng-intelligence`

分支：`codex/nanfeng-knowledge-production-checkpoint-20260727`

当前已提交基线：`21039d2`

当前工作区包含尚未提交的三入口重组、完整知识阅读模型、`>65`自动分类合同、来源全库正文搜索/搜索历史/一键加载全部、标题修改、软件内附件预览、来源回收站可见性修复、四入口统一单篇笔记卡与按需操作侧车、列表定位/正文搜索分工、主题管理定向操作、统一关联线、右侧主体`1.3×`阅读字号、动态场景文字对比度和新 BAT，不得覆盖、清理或丢弃。

先完整读取：

1. `AGENTS.md`
2. `docs/CURRENT_HANDOFF.md`
3. `docs/next-codex-prompt.md`
4. `docs/core-workspace-design-baseline.md`
5. `docs/core-workspace-acceptance-matrix.md`
6. `docs/core-workspace-requirements-traceability.md`
7. `design-qa.md`最后一节

再检查 Git 根目录、当前分支、`git status`和最近提交。不要先扫描全部聊天、旧审计或历史原型。

## 当前唯一任务

等待南烛枫运行：

`启动南枫知识库-四列表统一卡片验收.bat`

只处理这次真实桌面可见验收反馈，不主动扩展其他功能。

重点确认：

- `知识视图 / 来源档案 / 主题管理`是否真正对应文档职责，而不是旧页面换名；
- 自动整理是否为默认主路径，人工分类和结构维护是否只作为异常/次级入口；
- 三套场景皮肤是否保持此前确认的背景、外层留白、圆角浅色磨砂侧栏和完整半透明冷白主工作区；
- `原版浅色`是否继续使用深蓝侧栏；
- `知识视图`四个状态是否各自只展示假设、判断、笔记/来源、决策，并且跨时间事件只在判断演变出现；
- 正式假设/判断为空时，自动提取结果是否直接可读且能从次级入口调整；
- 来源档案删除后是否立即从列表消失，恢复后是否重新出现；
- 来源详情是否只保留查看知识成果和三点按钮，菜单是否包含收藏、持续跟踪、导出、完整正文和删除，并在空白点击或 Escape 时关闭；
- 来源列表顶部/滑块/当前是否只滚动不切换选中项，正文内功能是否明确为搜索；
- 搜索未加载来源正文中的短语是否仍能命中，搜索历史是否可回选/清空，`全部加载`是否只改变列表量而不决定搜索范围；
- 来源标题修改后列表、详情和兼容记录是否同步；
- 来源档案每一条来源（包括此前没有 legacy Record 的条目）是否都在悬停/聚焦后于卡片右侧中部显示收藏、完整导出、更多且不改变尺寸；更多菜单是否包含查看详情并支持外部点击/Escape关闭；
- 来源档案、我的收藏、持续跟踪、判断更新的列表面板外壳与列表卡是否使用同一视觉与信息顺序：语义图标、标题、主题、来源、右上日期；来源列表不再出现无意义的`0篇笔记`；
- 知识视图和主题管理的父子层级是否明显，右侧数量列是否上下对齐；
- 图片、PDF、文本/Markdown/JSON、音频和视频是否在软件最上层预览；不支持格式是否显示明确后备入口；
- 来源↔知识主题是否可以双向定位，并显示精确定位或诚实的定位说明；
- `主题管理`的新建、边界、别名、规则和关系建议是否分别进入对应真实操作；
- 批量导入导出是否只从`设置 → 数据与存储`进入，左侧不再出现手工标签和独立导入导出；
- 磁盘剩余量/总容量和弹窗皮肤是否正确；
- 三套场景背景上的页面标题、说明和加载态是否按背景明暗使用反差文字、按背景冷暖使用反向色相；回收站空状态是否使用冷白磨砂表面且卡内文字保持深色可读；
- 中栏、主阅读区和时间线滚动是否正常，窗口宽度和左侧导航是否稳定；
- 正式数据缺少对象时是否显示真实空状态，而不是静态假数据。
- 知识四状态、来源正文和记录详情的主体内容是否统一放大`1.3×`，且链接、按钮、元数据和左侧列表字号保持原样。

## 当前验证层级

- 设计：产品主规格、升级思路、用户文字和四张图的推导关系已固化。
- 代码：三个入口已按同一对象链重组。
- 自动验证：本轮前端138/138、定向Rust 1/1、Rust格式、TypeScript、Vite、Windows Tauri `--no-bundle`通过；既有Playwright 14/14没有在本轮重新执行。
- 最新可见入口：`启动南枫知识库-四列表统一卡片验收.bat`，独立目标`.runtime-qa/unified-note-list-v18-build`；Codex只执行过`--prepare / --verify`，未启动应用或打开正式数据。
- 既有隔离浏览器：1584×1000 下四个知识状态、来源↔知识双向定位和历史五套皮肤证据只作回归参考；当前必须按四套目录重新验收。
- 内置浏览器：导出提示在1280×720视口中心坐标(640,360)，2px橙框且位于导出弹窗上层；浏览器无正式知识桥，四状态正式内容未冒充已验收。
- BAT：v18已生成并通过`--prepare / --verify`；Codex 没有启动程序或打开正式数据。
- 真实桌面：当前v18已包含四入口统一列表卡、所有来源行快捷操作、主题/来源元数据、语义图标和层级数量列对齐，以及v17既有全库搜索/预览能力，仍待确认。
- 自动分类：最高候选严格`>65`自动采用，`45–65`保留候选，`<45`人工；正式库尚未批量验收。
- 正式 migration v4：尚未声明完成。

## 禁止项

- 不切回`main`；
- 不`reset --hard`、`clean`、`stash`或覆盖未知改动；
- 不把四张功能图的深蓝皮肤复制到三套场景皮肤；
- 不恢复旧记录中心、人工分类中心或 CRUD 首屏；
- 不后台打开、写入、覆盖或重建正式 SQLite/来源；
- 不扩大到模型 API、云同步、Inno Setup、安装包或发布；
- 不上传 GitHub；只有南烛枫明确说“上传”才操作正式 Release。

若南烛枫报告通过，只更新`docs/core-workspace-acceptance-matrix.md`和`docs/CURRENT_HANDOFF.md`中的真实桌面状态，然后停止。

若南烛枫报告问题，只复现并修复其明确指出的范围，做最小充分验证，重新生成当前交互修复 BAT，并再次等待可见验收。
