# 下一轮 Codex 可直接使用的提示词

继续南枫知识库当前真实工作区，不要从旧版本或旧 BAT 恢复合同。

仓库：`C:\Users\Administrator\Documents\软件开发\nanfeng-intelligence`

当前分支：`codex/nanfeng-knowledge-production-checkpoint-20260727`

当前已提交基线：`62e6a66`（工作区另含未提交的v82–v93持续成果，禁止覆盖）

工作区包含尚未提交的主题洞察、全部笔记、主题管理、图片预览、新笔记自动整理、固定材质、小卡片微交互、固定筛选磨砂连续性、正文搜索状态修复与合同治理成果；不得覆盖、清理、stash 或丢弃。

先完整读取：

1. `AGENTS.md`
2. `docs/CURRENT_HANDOFF.md`
3. `docs/product-brief.md`
4. `docs/architecture-governance.md`
5. `docs/core-workspace-design-baseline.md`
6. `docs/core-workspace-acceptance-matrix.md`

再核对仓库根、当前分支、HEAD 和工作区摘要。不要先扫描旧聊天、历史审计、旧原型或旧隔离包。

## 当前合同

- 用户可见核心入口只使用`主题洞察 / 全部笔记 / 主题管理`；`knowledge / sources / Record / 来源档案 / 知识视图`只作内部兼容标识或历史证据。
- 新笔记进入 App 唯一自动整理队列；高置信结果可撤销地归入主题，低置信、冲突或无匹配只留在全部笔记例外队列；侧栏不恢复`判断更新`。
- 当前主题洞察的材质处理方法是三套场景皮肤的全软件基线，不等同于沙漠灯笼。v87已按2026-08-09真实WebView2反馈替代v61低曝光RGB幅度：主题洞察、全部笔记、主题管理、收藏、跟踪、回收站与设置的前景内容/列表/判断卡共用明亮前景角色；固定低色度珍珠材质、alpha、低对比边缘渐退、中性抖动和外层单次模糊不变，大承托层、原版浅色、浮层/弹窗和主题整合主要来源阅读卡不参与提亮。禁止连续白描边、大承托整体增白、蓝绿补色、内部第二次`backdrop-filter`和运行时生成卡片RGB/透明度。
- `主题整合`右侧“关联笔记与来源”必须逐张保持完整标题与第二行来源类型、日期、置信度；数量增加只延长右列独立滚动内容，禁止压缩Grid行或裁切元数据。卡片主体继续在当前页切换正文，仅最右外链图标进入全部笔记。
- 全部笔记搜索、组合筛选、显示工具栏、整理状态和定位条直接复用`我的收藏`的非滚动控制区结构，全部位于列表滚动层之外；只有笔记卡列表滚动。禁止恢复`.knowledge-source-filters`、`sticky`、52px伪元素遮罩、负右边距或滚动条覆盖层。全部笔记自身的筛选功能继续保留。
- 所有紧凑小卡使用`NF-MICRO-CARD-LIFT-01`唯一规则：操作卡消费`lift`并保留按压，纯阅读卡消费`surface-lift`且不伪装按压。角色只挂每张最深层独立小卡，禁止整组/父容器/嵌套消费；悬浮一张时兄弟卡和外层必须静止，减少动态时取消位移和缩放。
- 全部笔记无论普通打开、跨页打开还是切换笔记都从正文顶部开始，旧证据锚点不得自动滚动；只有本次手动提交正文搜索才允许定位。正文搜索历史与卡片二历史分键保存，点击历史词只回填输入。
- 自动知识草案只从可独立理解的完整正文语义单元生成；聊天收尾、服务邀约、用户/助手标记和以`的话`等承接词开头的残句不得进入假设、判断、问题或决策。自动决策卡必须显示完整来源型标题，并按`形成依据 / 待确认建议 / 尚未执行 / 预期与结果`区分信息，不得用固定字符截断正文或伪造实际结果。
- 历史截图、旧 BAT、旧隔离包、旧版本号和旧入口名称不拥有现行合同。
- 历史附件搜索读取原文声明与已受控实体并集；当前笔记按唯一ID顺序物化缺失附件，目录、月份组和卡片壳立即完整显示；实际图片、音视频、PDF/TXT只在接近可视范围时挂载，音视频默认metadata。禁止恢复逐项“点击恢复”、只查`attachments`表或全页eager解码。
- 图片/视频时间线直接显示真实缩略图和视频代表帧；打开媒体分类后按来源顺序补齐缺失受控实体，点击继续复用最高层图片预览/视频播放器。不得退回文件名行，也不得另建播放器。
- Markdown/TXT等文本附件通过受控命令解码，`.md/.markdown`统一进入Markdown阅读器；正文内与最高层预览共享`AttachmentTextPreview`。禁止恢复本地文件iframe、WebView猜字符集、源码直出或整框横向滚动。
- 所有已受控附件统一显示`定位文件`：正文原位与最高层预览共享`AttachmentLocateButton`，前端只传附件ID，Rust重新校验数据库记录与受控附件目录后由Explorer直接选中文件。禁止页面传任意路径、只打开目录或按格式复制平台逻辑。
- 图片/视频最高层预览共用暗底媒体视口；中键和明确`适合屏幕`按钮均可复位。视频原生元素保持播放/控制所有权，缩放后由独立手柄平移；窗口只由CSS原生resize。禁止恢复64px控制区猜测和双重resize。
- 自定义右键菜单只接管笔记内容区；输入、可编辑区、链接、视频、音频、iframe、设置和导航保留系统菜单。
- 根目录唯一验收入口是`启动南枫知识库-当前验收.bat`；历史BAT已归档为不可执行文本。设置页“当前程序”显示版本、构建标签、EXE大小和运行文件自身SHA-256。

## 当前唯一任务

先由南烛枫关闭其他南枫知识库窗口并双击`启动南枫知识库-当前验收.bat`，核对设置页构建标签`v93-current-acceptance`及EXE SHA；再打开含真实MP4、图片、PDF和Markdown/TXT的长笔记，确认媒体复位、原生控制、多格式滚动加载、系统右键和Explorer定位。不得把隔离构建或自动测试冒充真实WebView2、Explorer或正式附件验收。

重点确认：

- 视频、图片、PDF、Markdown/TXT等正文原位预览都显示`定位文件`；
- 点击后Explorer打开的是受控附件所在目录，并直接选中目标文件；
- 进入最高层图片/视频/文档预览后，标题动作区的`定位文件`结果相同；
- 定位操作不打开、移动、改名或改写附件，预览、播放、缩放和关闭行为不回归。

## 当前验证层级

- 代码与合同：前端222/222、Rust96/96、TypeScript、Vite、Sites4/4通过；默认Playwright17/17、媒体专项3/3通过。
- Windows构建：v93隔离应用生成且`--prepare / --verify`通过；路径为`.runtime-qa/current-acceptance-v93-app/release/nanfeng-knowledge-base.exe`，35,704,320 bytes，SHA-256=`C8F865C3BFC93C748D85084575077C303341BD9B074F920C0F08FEBAEA9B7988`。
- 真实桌面：真实MP4声音/进度/音量/全屏、Explorer定位、正式Markdown和多格式长笔记压力待南烛枫确认。
- 正式数据：本轮未打开、未写入；migration v4/v5与完整正式导入仍不得声称通过。

## 禁止项

- 不切分支，不`reset --hard`、`clean`、`stash`或覆盖未知改动；
- 不读取、截图、展示旧 BAT 隔离包，除非明确做回归调查并标记为旧证据；
- 不后台打开、写入、覆盖或重建正式 SQLite/来源；
- 不恢复旧入口、假数据应用入口、人工判断更新入口或 CRUD 首屏；
- 不扩大到模型 API、云同步、安装、签名、GitHub 上传或 Release。

若南烛枫报告通过，只更新验收矩阵与当前交接中的真实桌面状态后停止；若报告问题，只修复明确范围并做最小充分验证。
