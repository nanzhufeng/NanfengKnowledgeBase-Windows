# 下一轮 Codex 唯一任务

继续开发“南枫知识库”。

仓库：`C:\Users\Administrator\Documents\软件开发\nanfeng-intelligence`

分支：`codex/nanfeng-knowledge-production-checkpoint-20260727`

代码 checkpoint：`16c0db6`

先完整读取：

1. `AGENTS.md`
2. `docs/CURRENT_HANDOFF.md`
3. `docs/next-codex-prompt.md`

再检查 `git status` 与最近提交。不要先扫描全部历史审计或聊天记录。

先简短汇报：

- 当前实现层级；
- 当前验证层级；
- 尚未完成项；
- 本轮边界。

## 唯一任务

承接南烛枫对新 BAT 的可见验收反馈，完成一次定向闭环。

- 如果南烛枫尚未运行 BAT，只说明入口和本轮重点，不自行打开正式应用。
- 如果南烛枫给出具体问题，先按截图/操作路径复现并定位，只修该问题及其同一根因影响范围。
- 修复后只运行受影响合同、TypeScript、Rust 定向测试和 `--no-bundle` 构建；必要时更新 BAT 指向新的隔离构建目录。
- 若验收通过，只更新交接与 checkpoint，不继续扩展功能。

验收重点：

1. `来源档案 / 主题结构 / 知识视图` 三入口是否清晰；
2. 自动分类是否为默认，人工确认是否仅出现在例外项；
3. `领域 → 主题 → 命题/竞争假设` 阅读层级是否符合预期；
4. 有效期、推翻条件、Evidence/Judgment 命题关联和决策账本是否可保存并重开持久化；
5. 图片/外部链接前台显示、详情弹窗和独立滚动是否无回归。

## 禁止项

- 不切回 `main`；
- 不扫描或改写全部历史审计；
- 不直接写正式 SQLite；
- 不删除、覆盖、清空或重建正式来源；
- 不恢复旧宽泛系统规则；
- 不让用户逐条寻找分类遗漏；
- 不扩大到模型 API、云同步、Inno Setup、安装包或发布；
- 不上传 GitHub；只有南烛枫明确说“上传”才操作正式 Release。

## 停止条件

完成南烛枫本次可见验收反馈的定向修复和最小充分验证后停止，明确区分：

- 已实现；
- 自动测试/构建通过；
- 用户真实桌面已确认；
- 尚未验证。
